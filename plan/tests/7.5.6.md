# TASK-7.5.6: Test PUT /api/tasks/:id updates only provided fields

**Section**: 7. HTTP Server - Tasks API
**Subsection**: 7.5.6
**Task ID**: TASK-7.5.6
**Parent Task**: TASK-7.5

## Description

Write a test to verify that `PUT /api/tasks/:id` updates only the fields provided in the request body, leaving other fields unchanged. This ensures partial updates work correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (PUT /api/tasks/:id endpoint), `src/task-service.ts` (updateTask method)

## Current State

- PUT /api/tasks/:id endpoint exists
- Partial update logic exists (or needs to be implemented)
- `tests/tasks-api.test.ts` exists
- Test for partial updates may not exist yet

**Important**: This test verifies partial update functionality.

## Checklist

### Preparation and Setup

- [ ] Review PUT /api/tasks/:id endpoint implementation
- [ ] Review TaskService.updateTask() method
- [ ] Review existing tests in `tests/tasks-api.test.ts`
- [ ] Use `createMockServer()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'PUT /api/tasks/:id updates only provided fields'`
  - [ ] Use same `describe` block as other /api/tasks/:id PUT tests
- [ ] Step 2: Set up test data
  - [ ] Create a test task in database with known values (prompt: 'original', order: 1, status: 0)
  - [ ] Store task ID and original values
- [ ] Step 3: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 4: Test endpoint with partial update
  - [ ] Make PUT request to `/api/tasks/{taskId}` with body: `{ prompt: 'updated prompt' }` (only prompt)
  - [ ] Verify response status is 200
  - [ ] Query database to verify task was updated
  - [ ] Verify prompt was updated to 'updated prompt'
  - [ ] Verify order remains 1 (unchanged)
  - [ ] Verify status remains 0 (unchanged)
- [ ] Step 5: Verify partial update
  - [ ] Assert only provided fields are updated
  - [ ] Assert other fields remain unchanged

### Specific Requirements

- [ ] Requirement 1: Only provided fields are updated
  - Acceptance: Fields in request body are updated
  - Acceptance: Fields not in request body remain unchanged
- [ ] Requirement 2: Update is correct
  - Acceptance: Updated values match request body
  - Acceptance: Unchanged values match original
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Update only order (verify prompt and status unchanged)
- [ ] Handle edge case 2: Update only status (verify prompt and order unchanged)

### Testing

- [ ] Write test in `tests/tasks-api.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Verify partial updates work correctly
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining partial update behavior

### Verification

- [ ] Verify test passes
- [ ] Verify partial updates work correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 7: HTTP Server - Tasks API
- **Execution Timing**: Must be completed after TASK-7.5.5
- **Dependencies**: TASK-7.5.5, TASK-1.1.4, TASK-1.1.6
- **Important Considerations**: 
  - Partial updates should only change provided fields
  - Use real database for integration test
- **Task Independence**: Can be done after TASK-7.5.5

## Related Tasks

- Previous: TASK-7.5.5
- Next: TASK-7.5.7
- Dependencies: TASK-7.5.5, TASK-1.1.4, TASK-1.1.6
- Related:
  - TASK-7.5: PUT /api/tasks/:id (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/tasks-api.test.ts`
2. Test passes
3. Partial updates are verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want partial updates so I can update only specific fields
- As a **client**, I want partial updates so I don't have to send all fields

**Automated Tests**:
- Test verifies only provided fields are updated
- Test verifies other fields remain unchanged
- Test passes
- All tests pass












