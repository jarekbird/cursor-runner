### Step 5.3: GET /api/working-directory/files
**Objective**: Test working directory file tree endpoint.

This task has been broken down into granular subtasks:

1. [TASK-5.3.1](./5.3.1.md): Test GET /api/working-directory/files returns 500 when TARGET_APP_PATH not set
2. [TASK-5.3.2](./5.3.2.md): Test GET /api/working-directory/files returns 404 when directory doesn't exist
3. [TASK-5.3.3](./5.3.3.md): Test GET /api/working-directory/files returns file tree when directory exists

**Note**: Complete all subtasks in order (5.3.1 → 5.3.2 → 5.3.3). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Extend `tests/server.test.ts`:
   - Add test: `GET /api/working-directory/files` returns 500 when `TARGET_APP_PATH` not set
   - Add test: `GET /api/working-directory/files` returns 404 when directory doesn't exist
   - Add test: `GET /api/working-directory/files` returns file tree when directory exists
   - Mock `process.env.TARGET_APP_PATH`, `filesystem.exists()`, and `FileTreeService.buildFileTree()`

**Tests**:
- Verify error cases
- Verify success case with mock file tree

**Verification**:
- Tests pass
- Error handling is appropriate

