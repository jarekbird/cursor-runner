# TASK-5.5.2: Test POST /api/:conversationId/message returns 400 when message is empty string

**Section**: 5. HTTP Server - Conversation API
**Subsection**: 5.5.2
**Task ID**: TASK-5.5.2
**Parent Task**: TASK-5.5

## Description

Write a test to verify that `POST /api/:conversationId/message` returns 400 status code when the `message` field is an empty string. This ensures empty messages are rejected.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /api/:conversationId/message endpoint, validation)

## Current State

- POST /api/:conversationId/message endpoint exists
- Validation logic exists (or needs to be implemented)
- `tests/server.test.ts` exists
- Test for empty message validation may not exist yet

**Important**: This test verifies empty string validation.

## Checklist

### Preparation and Setup

- [ ] Review POST /api/:conversationId/message endpoint implementation
- [ ] Review validation logic
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /api/:conversationId/message returns 400 when message is empty string'`
  - [ ] Use same `describe` block as other /api/:conversationId/message tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with empty message
  - [ ] Make POST request to `/api/test-conversation-id/message` with body: `{ message: '' }`
  - [ ] Verify response status is 400
  - [ ] Verify response body contains error message about empty message
- [ ] Step 4: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message is clear

### Specific Requirements

- [ ] Requirement 1: 400 status when message is empty
  - Acceptance: HTTP status code is 400
  - Acceptance: Error is returned immediately
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates message cannot be empty
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 5: HTTP Server - Conversation API
- **Execution Timing**: Must be completed after TASK-5.5.1
- **Dependencies**: TASK-5.5.1, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - Empty strings should be rejected
  - Validation should be strict
- **Task Independence**: Can be done after TASK-5.5.1

## Related Tasks

- Previous: TASK-5.5.1
- Next: TASK-5.5.3
- Dependencies: TASK-5.5.1, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-5.5: POST /api/:conversationId/message (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Validation is verified (400 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want empty messages rejected so I don't process invalid requests
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when message is empty string
- Test verifies error message is clear
- Test passes
- All tests pass












