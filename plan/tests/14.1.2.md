# TASK-14.1.2: Test GitService.repositoriesPath uses default when env not set

**Section**: 14. Filesystem & Workspace Services
**Subsection**: 14.1.2
**Task ID**: TASK-14.1.2
**Parent Task**: TASK-14.1

## Description

Write a test to verify that `GitService.repositoriesPath` uses the default value when the environment variable is not set. This ensures default behavior works correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/git-service.ts` (repositoriesPath property, default value)

## Current State

- GitService.repositoriesPath exists
- Default value exists
- `tests/git-service.test.ts` exists
- Test for default value may not exist yet

**Important**: This test verifies default value usage.

## Checklist

### Preparation and Setup

- [ ] Review GitService.repositoriesPath implementation
- [ ] Review default value definition
- [ ] Review existing tests in `tests/git-service.test.ts`
- [ ] Use test environment setup

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'repositoriesPath uses default when env not set'`
  - [ ] Use same `describe` block as other path resolution tests
- [ ] Step 2: Set up test environment
  - [ ] Use `beforeEach` to save original REPOSITORIES_PATH env var
  - [ ] Delete `REPOSITORIES_PATH` from `process.env`
  - [ ] Use `afterEach` to restore original env var
- [ ] Step 3: Test repositoriesPath without env var
  - [ ] Create GitService instance
  - [ ] Verify `repositoriesPath` property equals default value
  - [ ] Verify default value is used
- [ ] Step 4: Verify default usage
  - [ ] Assert repositoriesPath matches default value
  - [ ] Assert default is used when env var not set

### Specific Requirements

- [ ] Requirement 1: Default is used
  - Acceptance: repositoriesPath uses default when env var not set
  - Acceptance: Value matches default
- [ ] Requirement 2: Default is correct
  - Acceptance: Default value is appropriate
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Testing

- [ ] Write test in `tests/git-service.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining default value

### Verification

- [ ] Verify test passes
- [ ] Verify default value usage works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 14: Filesystem & Workspace Services
- **Execution Timing**: Must be completed after TASK-14.1.1
- **Dependencies**: TASK-14.1.1
- **Important Considerations**: 
  - Default should be sensible
  - Should work when env var not set
- **Task Independence**: Can be done after TASK-14.1.1

## Related Tasks

- Previous: TASK-14.1.1
- Next: TASK-14.1.3
- Dependencies: TASK-14.1.1
- Related:
  - TASK-14.1: GitService (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/git-service.test.ts`
2. Test passes
3. Default value usage is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want default value so system works without configuration
- As a **system**, I want sensible defaults so setup is simple

**Automated Tests**:
- Test verifies default is used when env not set
- Test passes
- All tests pass

