# TASK-8.1.1: Test GET /repositories/api/:repository/files returns 400 when repository param is missing

**Section**: 8. HTTP Server - Repository & Telegram Routes
**Subsection**: 8.1.1
**Task ID**: TASK-8.1.1
**Parent Task**: TASK-8.1

## Description

Write a test to verify that `GET /repositories/api/:repository/files` returns 400 status code when the repository parameter is missing from the URL. This ensures proper validation for the repository parameter.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /repositories/api/:repository/files endpoint, repository validation)

## Current State

- GET /repositories/api/:repository/files endpoint exists
- Validation logic exists (or needs to be implemented)
- `tests/server.test.ts` exists
- Test for repository validation may not exist yet

**Important**: This test verifies repository parameter validation.

## Checklist

### Preparation and Setup

- [ ] Review GET /repositories/api/:repository/files endpoint implementation
- [ ] Review validation logic for repository parameter
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /repositories/api/:repository/files returns 400 when repository param is missing'`
  - [ ] Use `describe` block for /repositories/api/:repository/files endpoint tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with missing repository
  - [ ] Test case 1: Make GET request to `/repositories/api//files` (empty repository) - verify 400
  - [ ] Test case 2: Verify endpoint requires repository parameter
- [ ] Step 4: Verify error responses
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates repository is required

### Specific Requirements

- [ ] Requirement 1: 400 status when repository is missing
  - Acceptance: HTTP status code is 400 for missing repository
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates repository is required
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 8: HTTP Server - Repository & Telegram Routes
- **Execution Timing**: Must be completed after TASK-7.6.3
- **Dependencies**: TASK-7.6.3, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - Repository parameter is required
- **Task Independence**: Can be done after TASK-7.6.3

## Related Tasks

- Previous: TASK-7.6.3
- Next: TASK-8.1.2
- Dependencies: TASK-7.6.3, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-8.1: GET /repositories/api/:repository/files (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Validation is verified (400 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want validation errors so I know when repository is missing
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when repository is missing
- Test passes
- All tests pass












