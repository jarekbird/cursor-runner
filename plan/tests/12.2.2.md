# TASK-12.2.2: Test TaskService.updateTask() updates only provided fields

**Section**: 12. Persistence & Migrations
**Subsection**: 12.2.2
**Task ID**: TASK-12.2.2
**Parent Task**: TASK-12.2

## Description

Write a test to verify that `TaskService.updateTask()` updates only the fields provided in the update object, leaving other fields unchanged. This ensures partial updates work correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/task-service.ts` (updateTask method, partial updates)

## Current State

- TaskService.updateTask() exists
- Partial update logic exists
- `tests/task-service.test.ts` exists
- Test for partial updates may not exist yet

**Important**: This test verifies partial update functionality.

## Checklist

### Preparation and Setup

- [ ] Review TaskService.updateTask() implementation
- [ ] Review existing tests in `tests/task-service.test.ts`
- [ ] Use `createTempSqliteDb()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'updateTask() updates only provided fields'`
  - [ ] Use same `describe` block as other update tests
- [ ] Step 2: Set up test database
  - [ ] Use `createTempSqliteDb()` helper in `beforeAll`
  - [ ] Run migrations to set up schema
- [ ] Step 3: Set up test data
  - [ ] Create test task with known values (prompt: 'original', order: 1, status: 0)
- [ ] Step 4: Test partial update
  - [ ] Call `updateTask()` with only prompt: 'updated'
  - [ ] Verify prompt is updated
  - [ ] Verify order remains 1 (unchanged)
  - [ ] Verify status remains 0 (unchanged)
- [ ] Step 5: Verify partial update
  - [ ] Assert only provided fields are updated
  - [ ] Assert other fields remain unchanged

### Specific Requirements

- [ ] Requirement 1: Only provided fields are updated
  - Acceptance: Fields in update object are updated
  - Acceptance: Fields not in update object remain unchanged
- [ ] Requirement 2: Update is correct
  - Acceptance: Updated values match update object
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Testing

- [ ] Write test in `tests/task-service.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining partial updates

### Verification

- [ ] Verify test passes
- [ ] Verify partial updates work correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 12: Persistence & Migrations
- **Execution Timing**: Must be completed after TASK-12.2.1
- **Dependencies**: TASK-12.2.1, TASK-1.1.2, TASK-1.1.4
- **Important Considerations**: 
  - Partial updates should only change provided fields
- **Task Independence**: Can be done after TASK-12.2.1

## Related Tasks

- Previous: TASK-12.2.1
- Next: TASK-12.2.3
- Dependencies: TASK-12.2.1, TASK-1.1.2, TASK-1.1.4
- Related:
  - TASK-12.2: TaskService - Update Operations (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/task-service.test.ts`
2. Test passes
3. Partial updates are verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want partial updates so I can update only specific fields
- As a **client**, I want partial updates so I don't have to send all fields

**Automated Tests**:
- Test verifies only provided fields are updated
- Test passes
- All tests pass

