# TASK-8.1.2: Test GET /repositories/api/:repository/files returns 404 when repository doesn't exist

**Section**: 8. HTTP Server - Repository & Telegram Routes
**Subsection**: 8.1.2
**Task ID**: TASK-8.1.2
**Parent Task**: TASK-8.1

## Description

Write a test to verify that `GET /repositories/api/:repository/files` returns 404 status code when the repository doesn't exist. This ensures proper error handling for non-existent repositories.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /repositories/api/:repository/files endpoint), `src/filesystem-service.ts` (exists method)

## Current State

- GET /repositories/api/:repository/files endpoint exists
- FilesystemService.exists() exists
- `tests/server.test.ts` exists
- Test for 404 handling may not exist yet

**Important**: This test verifies 404 error handling.

## Checklist

### Preparation and Setup

- [ ] Review GET /repositories/api/:repository/files endpoint implementation
- [ ] Review FilesystemService.exists() method
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /repositories/api/:repository/files returns 404 when repository doesn't exist'`
  - [ ] Use same `describe` block as other /repositories/api/:repository/files tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Mock `filesystem.exists()` to return false for non-existent repository
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with non-existent repository
  - [ ] Make GET request to `/repositories/api/non-existent-repo/files`
  - [ ] Verify response status is 404
  - [ ] Verify response body contains error information
- [ ] Step 4: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates repository doesn't exist

### Specific Requirements

- [ ] Requirement 1: Error returns 404 status
  - Acceptance: HTTP status code is 404 when repository doesn't exist
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates repository doesn't exist
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining 404 handling

### Verification

- [ ] Verify test passes
- [ ] Verify error handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 8: HTTP Server - Repository & Telegram Routes
- **Execution Timing**: Must be completed after TASK-8.1.1
- **Dependencies**: TASK-8.1.1, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - 404 is appropriate for not found resources
- **Task Independence**: Can be done after TASK-8.1.1

## Related Tasks

- Previous: TASK-8.1.1
- Next: TASK-8.1.3
- Dependencies: TASK-8.1.1, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-8.1: GET /repositories/api/:repository/files (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Error handling is verified (404 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want 404 errors so I know when repositories don't exist
- As a **client**, I want 404 responses so I can handle missing repositories

**Automated Tests**:
- Test verifies 404 status when repository doesn't exist
- Test passes
- All tests pass

