# TASK-11.3.4: Test ConversationService.addMessage() stores review-agent messages when debug enabled (env)

**Section**: 11. Redis-based Conversation Services
**Subsection**: 11.3.4
**Task ID**: TASK-11.3.4
**Parent Task**: TASK-11.3

## Description

Write a test to verify that `ConversationService.addMessage()` stores review-agent messages when debug is enabled via environment variable (when DB is unavailable). This ensures review-agent messages are stored when debug is on via env fallback.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/conversation-service.ts` (addMessage method, debug flag from env fallback)

## Current State

- ConversationService.addMessage() exists
- Debug flag from env fallback exists (or needs to be implemented)
- `tests/conversation-service.test.ts` exists
- Test for review-agent storage with env debug may not exist yet

**Important**: This test verifies review-agent storage when debug is enabled via env fallback.

## Checklist

### Preparation and Setup

- [ ] Review ConversationService.addMessage() implementation
- [ ] Review how debug flag falls back to env when DB unavailable
- [ ] Review existing tests in `tests/conversation-service.test.ts`
- [ ] Use `createMockRedisClient()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'addMessage() stores review-agent messages when debug enabled (env)'`
  - [ ] Use same `describe` block as other message storage tests
- [ ] Step 2: Set up test environment
  - [ ] Set `DEBUG` or relevant env var to enable debug
  - [ ] Use `beforeEach` to save original value
  - [ ] Use `afterEach` to restore original value
- [ ] Step 3: Set up mocks
  - [ ] Use `createMockRedisClient()` helper
  - [ ] Mock `isSystemSettingEnabled('debug')` to simulate DB unavailable, fallback to env
  - [ ] Track Redis operations
- [ ] Step 4: Test review-agent storage with env debug
  - [ ] Call `addMessage()` with review-agent message
  - [ ] Verify message IS stored in Redis
  - [ ] Verify Redis operations are performed
  - [ ] Verify env fallback is used
- [ ] Step 5: Verify storage
  - [ ] Assert review-agent message is stored when debug enabled via env
  - [ ] Assert env fallback is used when DB unavailable

### Specific Requirements

- [ ] Requirement 1: Review-agent messages are stored
  - Acceptance: Review-agent messages are stored when debug is enabled via env
  - Acceptance: Redis operations are performed
- [ ] Requirement 2: Env fallback is used
  - Acceptance: Env var is checked when DB unavailable
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests
  - Acceptance: Env var is restored after test

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Env var is not set (verify debug is false)

### Testing

- [ ] Write test in `tests/conversation-service.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining debug flag from env fallback

### Verification

- [ ] Verify test passes
- [ ] Verify review-agent storage works when debug enabled via env
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 11: Redis-based Conversation Services
- **Execution Timing**: Must be completed after TASK-11.3.3
- **Dependencies**: TASK-11.3.3, TASK-1.1.1, TASK-1.1.4
- **Important Considerations**: 
  - Debug flag should fall back to env when DB unavailable
  - Review-agent messages should be stored when debug is enabled via env
- **Task Independence**: Can be done after TASK-11.3.3

## Related Tasks

- Previous: TASK-11.3.3
- Next: TASK-11.4.1
- Dependencies: TASK-11.3.3, TASK-1.1.1, TASK-1.1.4
- Related:
  - TASK-11.3: ConversationService - Message Storage (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/conversation-service.test.ts`
2. Test passes
3. Review-agent storage with env debug is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want review-agent messages stored when debug is on via env so I can debug issues
- As a **system**, I want env fallback so I can control storage when DB is unavailable

**Automated Tests**:
- Test verifies review-agent messages are stored when debug enabled via env
- Test passes
- All tests pass

