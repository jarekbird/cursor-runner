# TASK-7.4.4: Test POST /api/tasks creates task with default order=0 and status=0

**Section**: 7. HTTP Server - Tasks API
**Subsection**: 7.4.4
**Task ID**: TASK-7.4.4
**Parent Task**: TASK-7.4

## Description

Write a test to verify that `POST /api/tasks` creates a task with default `order=0` and `status=0` when these fields are not provided. This ensures default values work correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /api/tasks endpoint), `src/task-service.ts` (createTask method)

## Current State

- POST /api/tasks endpoint exists
- Default value handling exists (or needs to be implemented)
- `tests/tasks-api.test.ts` exists
- Test for default values may not exist yet

**Important**: This test verifies default parameter handling.

## Checklist

### Preparation and Setup

- [ ] Review POST /api/tasks endpoint implementation
- [ ] Review TaskService.createTask() method
- [ ] Review how defaults are set
- [ ] Review existing tests in `tests/tasks-api.test.ts`
- [ ] Use `createMockServer()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /api/tasks creates task with default order=0 and status=0'`
  - [ ] Use same `describe` block as other /api/tasks POST tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint without order and status
  - [ ] Make POST request to `/api/tasks` with body: `{ prompt: 'test prompt' }` (no order, no status)
  - [ ] Verify response status is 201
  - [ ] Verify task was created in database
  - [ ] Verify created task has order: 0
  - [ ] Verify created task has status: 0
- [ ] Step 4: Verify default values
  - [ ] Query database to verify task has correct defaults
  - [ ] Assert order is 0
  - [ ] Assert status is 0

### Specific Requirements

- [ ] Requirement 1: Default order is 0
  - Acceptance: Task is created with order: 0 when not provided
  - Acceptance: Default value is correct
- [ ] Requirement 2: Default status is 0
  - Acceptance: Task is created with status: 0 when not provided
  - Acceptance: Default value is correct
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests
  - Acceptance: DB state is reset between tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: order is provided (verify provided value is used)
- [ ] Handle edge case 2: status is provided (verify provided value is used)

### Testing

- [ ] Write test in `tests/tasks-api.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Verify defaults are applied correctly
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining default values
- [ ] Document default order and status values

### Verification

- [ ] Verify test passes
- [ ] Verify default handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 7: HTTP Server - Tasks API
- **Execution Timing**: Must be completed after TASK-7.4.3
- **Dependencies**: TASK-7.4.3, TASK-1.1.4
- **Important Considerations**: 
  - Defaults should be order=0 and status=0
  - Defaults should only apply when fields are not provided
  - Use real database for integration test
- **Task Independence**: Can be done after TASK-7.4.3

## Related Tasks

- Previous: TASK-7.4.3
- Next: TASK-7.4.5
- Dependencies: TASK-7.4.3, TASK-1.1.4
- Related:
  - TASK-7.4: POST /api/tasks (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/tasks-api.test.ts`
2. Test passes
3. Default values are verified (order=0, status=0)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want default values so I don't have to specify order and status every time
- As a **client**, I want sensible defaults so requests are simpler

**Automated Tests**:
- Test verifies default order=0 is used
- Test verifies default status=0 is used
- Test passes
- All tests pass

