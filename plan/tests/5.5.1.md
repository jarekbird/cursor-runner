# TASK-5.5.1: Test POST /api/:conversationId/message returns 400 when message is missing

**Section**: 5. HTTP Server - Conversation API
**Subsection**: 5.5.1
**Task ID**: TASK-5.5.1
**Parent Task**: TASK-5.5

## Description

Write a test to verify that `POST /api/:conversationId/message` returns 400 status code when the `message` field is missing from the request body. This ensures proper validation for the message endpoint.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /api/:conversationId/message endpoint, validation)

## Current State

- POST /api/:conversationId/message endpoint exists
- Validation logic exists (or needs to be implemented)
- `tests/server.test.ts` exists
- Test for message validation may not exist yet

**Important**: This test verifies input validation.

## Checklist

### Preparation and Setup

- [ ] Review POST /api/:conversationId/message endpoint implementation
- [ ] Review validation logic
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /api/:conversationId/message returns 400 when message is missing'`
  - [ ] Use `describe` block for /api/:conversationId/message endpoint tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint without message
  - [ ] Make POST request to `/api/test-conversation-id/message` with body: `{}` (no message)
  - [ ] Verify response status is 400
  - [ ] Verify response body contains error message about missing message
- [ ] Step 4: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message is clear and actionable

### Specific Requirements

- [ ] Requirement 1: 400 status when message is missing
  - Acceptance: HTTP status code is 400
  - Acceptance: Error is returned immediately
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates message is required
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: message is null
  - Verify null is treated as missing

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 5: HTTP Server - Conversation API
- **Execution Timing**: Must be completed after TASK-5.4.5
- **Dependencies**: TASK-5.4.5, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - message is required for sending messages
  - Validation should happen before any processing
- **Task Independence**: Can be done after TASK-5.4.5

## Related Tasks

- Previous: TASK-5.4.5
- Next: TASK-5.5.2
- Dependencies: TASK-5.4.5, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-5.5: POST /api/:conversationId/message (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Validation is verified (400 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want validation errors so I know when required fields are missing
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when message is missing
- Test verifies error message is clear
- Test passes
- All tests pass








