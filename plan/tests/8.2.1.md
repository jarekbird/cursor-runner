# TASK-8.2.1: Test POST /telegram/webhook handles message update type

**Section**: 8. HTTP Server - Repository & Telegram Routes
**Subsection**: 8.2.1
**Task ID**: TASK-8.2.1
**Parent Task**: TASK-8.2

## Description

Write a test to verify that `POST /telegram/webhook` handles `message` update type correctly. This ensures Telegram message updates are processed properly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /telegram/webhook endpoint, message handling)

## Current State

- POST /telegram/webhook endpoint exists
- Message update handling exists (or needs to be implemented)
- `tests/server.test.ts` exists
- Test for message update type may not exist yet

**Important**: This test verifies message update type handling.

## Checklist

### Preparation and Setup

- [ ] Review POST /telegram/webhook endpoint implementation
- [ ] Review how message updates are processed
- [ ] Review Telegram webhook update structure
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /telegram/webhook handles message update type'`
  - [ ] Use `describe` block for /telegram/webhook endpoint tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Mock Telegram webhook handler or message processing logic
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with message update
  - [ ] Create Telegram webhook payload with `message` update type
  - [ ] Make POST request to `/telegram/webhook` with payload
  - [ ] Verify response status is 200
  - [ ] Verify message handler was called
  - [ ] Verify message was processed correctly
- [ ] Step 4: Verify message handling
  - [ ] Assert message handler receives correct data
  - [ ] Assert response indicates success

### Specific Requirements

- [ ] Requirement 1: Message update is handled
  - Acceptance: Message handler is called with correct data
  - Acceptance: Message is processed correctly
- [ ] Requirement 2: Response status is 200
  - Acceptance: HTTP status code is 200
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Message without text (verify handling)
- [ ] Handle edge case 2: Message from bot (verify if ignored)

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining message update handling

### Verification

- [ ] Verify test passes
- [ ] Verify message handling works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 8: HTTP Server - Repository & Telegram Routes
- **Execution Timing**: Must be completed after TASK-8.1.3
- **Dependencies**: TASK-8.1.3, TASK-1.1.4, TASK-1.1.6
- **Important Considerations**: 
  - Telegram webhook payload structure should match Telegram API
  - Message updates should trigger conversation handling
- **Task Independence**: Can be done after TASK-8.1.3

## Related Tasks

- Previous: TASK-8.1.3
- Next: TASK-8.2.2
- Dependencies: TASK-8.1.3, TASK-1.1.4, TASK-1.1.6
- Related:
  - TASK-8.2: POST /telegram/webhook (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Message update handling is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want message updates handled so Telegram messages are processed
- As a **system**, I want message handling so users can interact via Telegram

**Automated Tests**:
- Test verifies message update type is handled
- Test verifies message is processed correctly
- Test passes
- All tests pass












