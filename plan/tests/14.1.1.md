# TASK-14.1.1: Test GitService.repositoriesPath uses env var when set

**Section**: 14. Filesystem & Workspace Services
**Subsection**: 14.1.1
**Task ID**: TASK-14.1.1
**Parent Task**: TASK-14.1

## Description

Write a test to verify that `GitService.repositoriesPath` uses the environment variable value when it is set. This ensures environment variable configuration works correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/git-service.ts` (repositoriesPath property, env var reading)

## Current State

- GitService.repositoriesPath exists
- Environment variable reading exists
- `tests/git-service.test.ts` exists (or needs to be created)
- Test for env var usage may not exist yet

**Important**: This test verifies environment variable configuration.

## Checklist

### Preparation and Setup

- [ ] Review GitService.repositoriesPath implementation
- [ ] Review environment variable reading logic
- [ ] Review existing tests in `tests/git-service.test.ts`
- [ ] Use test environment setup

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'repositoriesPath uses env var when set'`
  - [ ] Use `describe` block for GitService path resolution tests
- [ ] Step 2: Set up test environment
  - [ ] Use `beforeEach` to save original REPOSITORIES_PATH env var
  - [ ] Set `process.env.REPOSITORIES_PATH` to test path: '/test/repositories'
  - [ ] Use `afterEach` to restore original env var
- [ ] Step 3: Test repositoriesPath with env var
  - [ ] Create GitService instance
  - [ ] Verify `repositoriesPath` property equals '/test/repositories'
  - [ ] Verify env var value is used
- [ ] Step 4: Verify env var usage
  - [ ] Assert repositoriesPath matches env var value
  - [ ] Assert env var is read correctly

### Specific Requirements

- [ ] Requirement 1: Env var is used
  - Acceptance: repositoriesPath uses env var value when set
  - Acceptance: Value matches env var
- [ ] Requirement 2: Reading is correct
  - Acceptance: Env var is read correctly
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests
  - Acceptance: Env var is restored after test

### Testing

- [ ] Write test in `tests/git-service.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining env var usage

### Verification

- [ ] Verify test passes
- [ ] Verify env var usage works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 14: Filesystem & Workspace Services
- **Execution Timing**: Must be completed after TASK-13.3.6
- **Dependencies**: TASK-13.3.6
- **Important Considerations**: 
  - Env var should take precedence when set
  - Must properly clean up env vars
- **Task Independence**: Can be done after TASK-13.3.6

## Related Tasks

- Previous: TASK-13.3.6
- Next: TASK-14.1.2
- Dependencies: TASK-13.3.6
- Related:
  - TASK-14.1: GitService (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/git-service.test.ts`
2. Test passes
3. Env var usage is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want env var configuration so I can customize repositories path
- As a **system**, I want env var support so configuration is flexible

**Automated Tests**:
- Test verifies env var is used when set
- Test passes
- All tests pass








