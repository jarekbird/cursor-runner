### Step 13.1: System Settings - Database & Env Fallback
**Objective**: Test system settings reading.

This task has been broken down into granular subtasks:

1. [TASK-13.1.1](./13.1.1.md): Test isSystemSettingEnabled() reads from DB when available
2. [TASK-13.1.2](./13.1.2.md): Test isSystemSettingEnabled() falls back to env for debug when DB unavailable
3. [TASK-13.1.3](./13.1.3.md): Test isSystemSettingEnabled() returns false when setting not found
4. [TASK-13.1.4](./13.1.4.md): Test closeDatabase() closes without throwing

**Note**: Complete all subtasks in order (13.1.1 → 13.1.2 → 13.1.3 → 13.1.4). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Extend `tests/system-settings.test.ts`:
   - Add test: `isSystemSettingEnabled()` reads from DB when available
   - Add test: `isSystemSettingEnabled()` falls back to env for `debug` when DB unavailable
   - Add test: `isSystemSettingEnabled()` returns false when setting not found
   - Add test: `closeDatabase()` closes without throwing
   - Use temp SQLite DB and mock DB unavailability

**Tests**:
- Verify DB reading
- Verify fallback behavior
- Verify cleanup

**Verification**:
- Tests pass

