# TASK-6.3.4: Test POST /api/agent/:id/message sets default source='text' when not provided

**Section**: 6. HTTP Server - Agent Conversation API
**Subsection**: 6.3.4
**Task ID**: TASK-6.3.4
**Parent Task**: TASK-6.3

## Description

Write a test to verify that `POST /api/agent/:id/message` sets default `source='text'` when it is not provided in the request body. This ensures default values work correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /api/agent/:id/message endpoint, source default)

## Current State

- POST /api/agent/:id/message endpoint exists
- source default handling exists (or needs to be implemented)
- `tests/agent-conversation-api.integration.test.ts` exists
- Test for source default may not exist yet

**Important**: This test verifies default parameter handling.

## Checklist

### Preparation and Setup

- [ ] Review POST /api/agent/:id/message endpoint implementation
- [ ] Review how source default is set
- [ ] Review existing tests in `tests/agent-conversation-api.integration.test.ts`
- [ ] Use `createMockServer()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /api/agent/:id/message sets default source=text when not provided'`
  - [ ] Use same `describe` block as other /api/agent/:id/message tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Mock `agentConversationService.getConversation()` to return test conversation
  - [ ] Mock `agentConversationService.addMessage()` to track source parameter
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint without source
  - [ ] Make POST request to `/api/agent/test-id/message` with body: `{ role: 'user', content: 'test' }` (no source)
  - [ ] Verify addMessage() was called with source: 'text'
  - [ ] Verify response is successful
- [ ] Step 4: Verify default value
  - [ ] Assert addMessage() receives source: 'text' when not provided
  - [ ] Assert default is used when not provided

### Specific Requirements

- [ ] Requirement 1: Default source is 'text'
  - Acceptance: addMessage() is called with source: 'text' when not provided
  - Acceptance: Default value is correct
- [ ] Requirement 2: Default is used correctly
  - Acceptance: Default is applied when source is missing
  - Acceptance: Default is not used when source is provided
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: source is provided
  - Verify provided source is used (not default)

### Testing

- [ ] Write test in `tests/agent-conversation-api.integration.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Verify default source is used correctly
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining default value
- [ ] Document default source value

### Verification

- [ ] Verify test passes
- [ ] Verify default handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 6: HTTP Server - Agent Conversation API
- **Execution Timing**: Must be completed after TASK-6.3.3
- **Dependencies**: TASK-6.3.3, TASK-1.1.4
- **Important Considerations**: 
  - Default should be 'text' as specified
  - Default should only apply when source is not provided
- **Task Independence**: Can be done after TASK-6.3.3

## Related Tasks

- Previous: TASK-6.3.3
- Next: TASK-6.4.1
- Dependencies: TASK-6.3.3, TASK-1.1.4
- Related:
  - TASK-6.3: POST /api/agent/:id/message (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/agent-conversation-api.integration.test.ts`
2. Test passes
3. Default source is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want default source='text' so I don't have to specify it every time
- As a **client**, I want sensible defaults so requests are simpler

**Automated Tests**:
- Test verifies default source='text' is used
- Test passes
- All tests pass








