# TASK-7.2.4: Test GET /api/tasks?status=invalid returns 400

**Section**: 7. HTTP Server - Tasks API
**Subsection**: 7.2.4
**Task ID**: TASK-7.2.4
**Parent Task**: TASK-7.2

## Description

Write a test to verify that `GET /api/tasks?status=invalid` returns 400 status code when the status parameter is invalid. This ensures proper validation for status filter.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /api/tasks endpoint, status validation)

## Current State

- GET /api/tasks endpoint exists
- Validation logic exists (or needs to be implemented)
- `tests/tasks-api.test.ts` exists
- Test for invalid status validation may not exist yet

**Important**: This test verifies status parameter validation.

## Checklist

### Preparation and Setup

- [ ] Review GET /api/tasks endpoint implementation
- [ ] Review validation logic for status parameter
- [ ] Review existing tests in `tests/tasks-api.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /api/tasks?status=invalid returns 400'`
  - [ ] Use same `describe` block as other /api/tasks tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with invalid status
  - [ ] Test case 1: Make GET request to `/api/tasks?status=invalid` - verify 400
  - [ ] Test case 2: Make GET request to `/api/tasks?status=abc` - verify 400
  - [ ] Test case 3: Make GET request to `/api/tasks?status=-1` - verify 400 (if negative is invalid)
  - [ ] Test case 4: Make GET request to `/api/tasks?status=999` - verify 400 (if out of range)
- [ ] Step 4: Verify error responses
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates status is invalid

### Specific Requirements

- [ ] Requirement 1: 400 status for invalid status
  - Acceptance: HTTP status code is 400 for invalid status values
  - Acceptance: Error is returned immediately
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates status is invalid
  - Acceptance: Error message indicates valid values (if applicable)
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: status is empty string
  - Verify empty string is treated as invalid
- [ ] Handle edge case 2: status is valid number as string ('0', '1')
  - Verify these are accepted (not invalid)

### Testing

- [ ] Write test in `tests/tasks-api.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation
- [ ] Document valid status values

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 7: HTTP Server - Tasks API
- **Execution Timing**: Must be completed after TASK-7.2.3
- **Dependencies**: TASK-7.2.3, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - Status must be a valid number (0 or 1)
  - Validation should happen before database query
- **Task Independence**: Can be done after TASK-7.2.3

## Related Tasks

- Previous: TASK-7.2.3
- Next: TASK-7.3.1
- Dependencies: TASK-7.2.3, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-7.2: GET /api/tasks (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/tasks-api.test.ts`
2. Test passes
3. Validation is verified (400 status for invalid status values)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want validation errors so I know when status parameter is invalid
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when status is invalid
- Test verifies error message is clear
- Test passes
- All tests pass

