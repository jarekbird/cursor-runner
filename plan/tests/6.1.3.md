# TASK-6.1.3: Test GET /api/agent/list returns 400 when sortBy is invalid

**Section**: 6. HTTP Server - Agent Conversation API
**Subsection**: 6.1.3
**Task ID**: TASK-6.1.3
**Parent Task**: TASK-6.1

## Description

Write a test to verify that `GET /api/agent/list` returns 400 status code when the `sortBy` query parameter is invalid (not in the allowed list of sort fields). This ensures proper validation for sorting parameters.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /api/agent/list endpoint, sortBy validation)

## Current State

- GET /api/agent/list endpoint exists
- Validation logic exists (or needs to be implemented)
- `tests/agent-conversation-api.integration.test.ts` exists
- Test for sortBy validation may not exist yet

**Important**: This test verifies sorting parameter validation.

## Checklist

### Preparation and Setup

- [ ] Review GET /api/agent/list endpoint implementation
- [ ] Review validation logic for sortBy parameter
- [ ] Review allowed sortBy values (e.g., 'createdAt', 'updatedAt', 'id')
- [ ] Review existing tests in `tests/agent-conversation-api.integration.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /api/agent/list returns 400 when sortBy is invalid'`
  - [ ] Use same `describe` block as other /api/agent/list tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with invalid sortBy values
  - [ ] Test case 1: Make GET request to `/api/agent/list?sortBy=invalidField` - verify 400
  - [ ] Test case 2: Make GET request to `/api/agent/list?sortBy=` - verify 400 (if empty string is invalid)
  - [ ] Test case 3: Make GET request to `/api/agent/list?sortBy=randomString` - verify 400
- [ ] Step 4: Verify error responses
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates sortBy is invalid
  - [ ] Verify error message lists allowed values (if applicable)

### Specific Requirements

- [ ] Requirement 1: 400 status for invalid sortBy
  - Acceptance: HTTP status code is 400 for values not in allowed list
  - Acceptance: Error is returned immediately
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates sortBy is invalid
  - Acceptance: Error message lists allowed values (helpful for clients)
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: sortBy is undefined (should use default, not error)
- [ ] Handle edge case 2: sortBy is case-sensitive vs case-insensitive (verify expected behavior)

### Testing

- [ ] Write test in `tests/agent-conversation-api.integration.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation
- [ ] Document allowed sortBy values

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 6: HTTP Server - Agent Conversation API
- **Execution Timing**: Must be completed after TASK-6.1.2
- **Dependencies**: TASK-6.1.2, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - sortBy must be in allowed list
  - Allowed values should be documented
- **Task Independence**: Can be done after TASK-6.1.2

## Related Tasks

- Previous: TASK-6.1.2
- Next: TASK-6.1.4
- Dependencies: TASK-6.1.2, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-6.1: GET /api/agent/list (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/agent-conversation-api.integration.test.ts`
2. Test passes
3. Validation is verified (400 status for invalid sortBy values)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want validation errors so I know when sorting parameters are invalid
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when sortBy is invalid
- Test verifies error message is clear
- Test passes
- All tests pass








