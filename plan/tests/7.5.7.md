# TASK-7.5.7: Test PUT /api/tasks/:id updates updatedat timestamp

**Section**: 7. HTTP Server - Tasks API
**Subsection**: 7.5.7
**Task ID**: TASK-7.5.7
**Parent Task**: TASK-7.5

## Description

Write a test to verify that `PUT /api/tasks/:id` updates the `updatedat` timestamp when a task is updated. This ensures timestamps are maintained correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (PUT /api/tasks/:id endpoint), `src/task-service.ts` (updateTask method)

## Current State

- PUT /api/tasks/:id endpoint exists
- Timestamp update logic exists (or needs to be implemented)
- `tests/tasks-api.test.ts` exists
- Test for timestamp updates may not exist yet

**Important**: This test verifies timestamp maintenance.

## Checklist

### Preparation and Setup

- [ ] Review PUT /api/tasks/:id endpoint implementation
- [ ] Review TaskService.updateTask() method
- [ ] Review how updatedat is set
- [ ] Review existing tests in `tests/tasks-api.test.ts`
- [ ] Use `createMockServer()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'PUT /api/tasks/:id updates updatedat timestamp'`
  - [ ] Use same `describe` block as other /api/tasks/:id PUT tests
- [ ] Step 2: Set up test data
  - [ ] Create a test task in database
  - [ ] Record original `updatedat` timestamp
  - [ ] Wait a small amount of time (e.g., 10ms) to ensure timestamp difference
- [ ] Step 3: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 4: Test endpoint and verify timestamp update
  - [ ] Make PUT request to `/api/tasks/{taskId}` with body: `{ prompt: 'updated' }`
  - [ ] Verify response status is 200
  - [ ] Query database to get updated task
  - [ ] Verify `updatedat` timestamp is newer than original
  - [ ] Verify `updatedat` is a valid timestamp
- [ ] Step 5: Verify timestamp update
  - [ ] Assert updatedat is updated
  - [ ] Assert updatedat is more recent than original

### Specific Requirements

- [ ] Requirement 1: updatedat is updated
  - Acceptance: updatedat timestamp changes when task is updated
  - Acceptance: updatedat is more recent than original
- [ ] Requirement 2: Timestamp is valid
  - Acceptance: updatedat is a valid date/timestamp
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Multiple rapid updates (verify timestamp increments)

### Testing

- [ ] Write test in `tests/tasks-api.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Verify timestamp updates correctly
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining timestamp behavior

### Verification

- [ ] Verify test passes
- [ ] Verify timestamp updates correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 7: HTTP Server - Tasks API
- **Execution Timing**: Must be completed after TASK-7.5.6
- **Dependencies**: TASK-7.5.6, TASK-1.1.4, TASK-1.1.6
- **Important Considerations**: 
  - updatedat should be automatically updated on any change
  - Use real database for integration test
- **Task Independence**: Can be done after TASK-7.5.6

## Related Tasks

- Previous: TASK-7.5.6
- Next: TASK-7.5.8
- Dependencies: TASK-7.5.6, TASK-1.1.4, TASK-1.1.6
- Related:
  - TASK-7.5: PUT /api/tasks/:id (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/tasks-api.test.ts`
2. Test passes
3. Timestamp update is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want updatedat to update so I can track when tasks change
- As a **system**, I want automatic timestamp updates so I maintain accurate records

**Automated Tests**:
- Test verifies updatedat is updated
- Test verifies timestamp is more recent
- Test passes
- All tests pass












