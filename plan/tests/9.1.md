### Step 9.1: CursorExecutionService - Repository Validation
**Objective**: Test repository validation logic.

This task has been broken down into granular subtasks:

1. [TASK-9.1.1](./9.1.1.md): Test execute() returns error when TARGET_APP_PATH is missing and no repository provided
2. [TASK-9.1.2](./9.1.2.md): Test execute() returns error when repository path doesn't exist
3. [TASK-9.1.3](./9.1.3.md): Test execute() returns error when workspace is not trusted

**Note**: Complete all subtasks in order (9.1.1 → 9.1.2 → 9.1.3). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Create or extend `tests/cursor-execution-service.test.ts`:
   - Add test: `execute()` returns error when `TARGET_APP_PATH` is missing and no repository provided
   - Add test: `execute()` returns error when repository path doesn't exist
   - Add test: `execute()` returns error when workspace is not trusted
   - Mock `GitService`, `FilesystemService`, `WorkspaceTrustService`

**Tests**:
- Verify all validation cases
- Verify error response structure

**Verification**:
- Tests pass
- Validation is comprehensive

