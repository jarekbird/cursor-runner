# TASK-7.4.3: Test POST /api/tasks returns 400 when prompt is not a string

**Section**: 7. HTTP Server - Tasks API
**Subsection**: 7.4.3
**Task ID**: TASK-7.4.3
**Parent Task**: TASK-7.4

## Description

Write a test to verify that `POST /api/tasks` returns 400 status code when the `prompt` field is not a string type. This ensures type validation works correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /api/tasks endpoint, prompt type validation)

## Current State

- POST /api/tasks endpoint exists
- Type validation exists (or needs to be implemented)
- `tests/tasks-api.test.ts` exists
- Test for prompt type validation may not exist yet

**Important**: This test verifies type validation.

## Checklist

### Preparation and Setup

- [ ] Review POST /api/tasks endpoint implementation
- [ ] Review type validation logic for prompt parameter
- [ ] Review existing tests in `tests/tasks-api.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /api/tasks returns 400 when prompt is not a string'`
  - [ ] Use same `describe` block as other /api/tasks POST tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with invalid prompt types
  - [ ] Test case 1: Make POST request with body: `{ prompt: 123 }` (number) - verify 400
  - [ ] Test case 2: Make POST request with body: `{ prompt: true }` (boolean) - verify 400
  - [ ] Test case 3: Make POST request with body: `{ prompt: {} }` (object) - verify 400
  - [ ] Test case 4: Make POST request with body: `{ prompt: [] }` (array) - verify 400
- [ ] Step 4: Verify error responses
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates prompt must be a string

### Specific Requirements

- [ ] Requirement 1: 400 status for non-string prompt
  - Acceptance: HTTP status code is 400 for non-string prompt values
  - Acceptance: Error is returned immediately
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates prompt must be a string
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: prompt is null (may be separate from missing)
- [ ] Handle edge case 2: prompt is undefined (may be separate from missing)

### Testing

- [ ] Write test in `tests/tasks-api.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining type validation

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 7: HTTP Server - Tasks API
- **Execution Timing**: Must be completed after TASK-7.4.2
- **Dependencies**: TASK-7.4.2, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - prompt must be a string type
  - Type validation should happen before any processing
- **Task Independence**: Can be done after TASK-7.4.2

## Related Tasks

- Previous: TASK-7.4.2
- Next: TASK-7.4.4
- Dependencies: TASK-7.4.2, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-7.4: POST /api/tasks (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/tasks-api.test.ts`
2. Test passes
3. Type validation is verified (400 status for non-string prompt)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want type validation so I know when prompt type is invalid
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when prompt is not a string
- Test verifies error message is clear
- Test passes
- All tests pass












