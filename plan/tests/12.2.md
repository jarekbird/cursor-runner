### Step 12.2: TaskService - Update Operations
**Objective**: Test task updates.

This task has been broken down into granular subtasks:

1. [TASK-12.2.1](./12.2.1.md): Test updateTask() with no changes returns existing task
2. [TASK-12.2.2](./12.2.2.md): Test updateTask() updates only provided fields
3. [TASK-12.2.3](./12.2.3.md): Test updateTask() updates updatedat timestamp
4. [TASK-12.2.4](./12.2.4.md): Test deleteTask() returns true only when row deleted
5. [TASK-12.2.5](./12.2.5.md): Test close() closes DB and allows re-open

**Note**: Complete all subtasks in order (12.2.1 → 12.2.2 → 12.2.3 → 12.2.4 → 12.2.5). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Extend `tests/task-service.test.ts`:
   - Add test: `updateTask()` with no changes returns existing task
   - Add test: `updateTask()` updates only provided fields
   - Add test: `updateTask()` updates `updatedat` timestamp
   - Add test: `deleteTask()` returns true only when row deleted
   - Add test: `close()` closes DB and allows re-open

**Tests**:
- Verify update behavior
- Verify timestamp updates
- Verify deletion
- Verify cleanup

**Verification**:
- Tests pass

