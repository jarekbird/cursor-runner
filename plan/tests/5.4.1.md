# TASK-5.4.1: Test GET /api/:conversationId returns 404 when conversation not found

**Section**: 5. HTTP Server - Conversation API
**Subsection**: 5.4.1
**Task ID**: TASK-5.4.1
**Parent Task**: TASK-5.4

## Description

Write a test to verify that `GET /api/:conversationId` returns 404 status code when the conversation is not found. This ensures proper error handling for non-existent conversations.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /api/:conversationId endpoint), `src/conversation-service.ts` (getConversationById method)

## Current State

- GET /api/:conversationId endpoint exists
- ConversationService.getConversationById() exists
- `tests/server.test.ts` exists
- Test for 404 handling may not exist yet

**Important**: This test verifies 404 error handling.

## Checklist

### Preparation and Setup

- [ ] Review GET /api/:conversationId endpoint implementation
- [ ] Review ConversationService.getConversationById() method
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /api/:conversationId returns 404 when conversation not found'`
  - [ ] Use `describe` block for /api/:conversationId endpoint tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Mock `conversationService.getConversationById()` to return null or throw not found error
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with non-existent conversation
  - [ ] Make GET request to `/api/non-existent-id`
  - [ ] Verify response status is 404
  - [ ] Verify response body contains error information
- [ ] Step 4: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates conversation not found

### Specific Requirements

- [ ] Requirement 1: Error returns 404 status
  - Acceptance: HTTP status code is 404 when conversation not found
  - Acceptance: Error is caught and handled
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates conversation not found
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining 404 handling

### Verification

- [ ] Verify test passes
- [ ] Verify error handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 5: HTTP Server - Conversation API
- **Execution Timing**: Must be completed after TASK-5.3.3
- **Dependencies**: TASK-5.3.3, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - 404 is appropriate for not found resources
- **Task Independence**: Can be done after TASK-5.3.3

## Related Tasks

- Previous: TASK-5.3.3
- Next: TASK-5.4.2
- Dependencies: TASK-5.3.3, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-5.4: GET /api/:conversationId (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Error handling is verified (404 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want 404 errors so I know when conversations don't exist
- As a **client**, I want 404 responses so I can handle missing conversations

**Automated Tests**:
- Test verifies 404 status when conversation not found
- Test passes
- All tests pass

