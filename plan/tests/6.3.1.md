# TASK-6.3.1: Test POST /api/agent/:id/message returns 400 when role is missing

**Section**: 6. HTTP Server - Agent Conversation API
**Subsection**: 6.3.1
**Task ID**: TASK-6.3.1
**Parent Task**: TASK-6.3

## Description

Write a test to verify that `POST /api/agent/:id/message` returns 400 status code when the `role` field is missing from the request body. This ensures proper validation for message fields.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /api/agent/:id/message endpoint, role validation)

## Current State

- POST /api/agent/:id/message endpoint exists
- Validation logic exists (or needs to be implemented)
- `tests/agent-conversation-api.integration.test.ts` exists
- Test for role validation may not exist yet

**Important**: This test verifies input validation.

## Checklist

### Preparation and Setup

- [ ] Review POST /api/agent/:id/message endpoint implementation
- [ ] Review validation logic for role parameter
- [ ] Review existing tests in `tests/agent-conversation-api.integration.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /api/agent/:id/message returns 400 when role is missing'`
  - [ ] Use `describe` block for /api/agent/:id/message endpoint tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint without role
  - [ ] Make POST request to `/api/agent/test-id/message` with body: `{ content: 'test message' }` (no role)
  - [ ] Verify response status is 400
  - [ ] Verify response body contains error message about missing role
- [ ] Step 4: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message is clear and actionable

### Specific Requirements

- [ ] Requirement 1: 400 status when role is missing
  - Acceptance: HTTP status code is 400
  - Acceptance: Error is returned immediately
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates role is required
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: role is null
  - Verify null is treated as missing

### Testing

- [ ] Write test in `tests/agent-conversation-api.integration.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 6: HTTP Server - Agent Conversation API
- **Execution Timing**: Must be completed after TASK-6.2.3
- **Dependencies**: TASK-6.2.3, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - role is required for messages
  - Validation should happen before any processing
- **Task Independence**: Can be done after TASK-6.2.3

## Related Tasks

- Previous: TASK-6.2.3
- Next: TASK-6.3.2
- Dependencies: TASK-6.2.3, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-6.3: POST /api/agent/:id/message (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/agent-conversation-api.integration.test.ts`
2. Test passes
3. Validation is verified (400 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want validation errors so I know when required fields are missing
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when role is missing
- Test verifies error message is clear
- Test passes
- All tests pass

