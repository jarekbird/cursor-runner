# TASK-7.4.2: Test POST /api/tasks returns 400 when prompt is empty string

**Section**: 7. HTTP Server - Tasks API
**Subsection**: 7.4.2
**Task ID**: TASK-7.4.2
**Parent Task**: TASK-7.4

## Description

Write a test to verify that `POST /api/tasks` returns 400 status code when the `prompt` field is an empty string. This ensures empty prompts are rejected.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /api/tasks endpoint, prompt validation)

## Current State

- POST /api/tasks endpoint exists
- Validation logic exists (or needs to be implemented)
- `tests/tasks-api.test.ts` exists
- Test for empty prompt validation may not exist yet

**Important**: This test verifies empty string validation.

## Checklist

### Preparation and Setup

- [ ] Review POST /api/tasks endpoint implementation
- [ ] Review validation logic for prompt parameter
- [ ] Review existing tests in `tests/tasks-api.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /api/tasks returns 400 when prompt is empty string'`
  - [ ] Use same `describe` block as other /api/tasks POST tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with empty prompt
  - [ ] Make POST request to `/api/tasks` with body: `{ prompt: '' }`
  - [ ] Verify response status is 400
  - [ ] Verify response body contains error message about empty prompt
- [ ] Step 4: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message is clear

### Specific Requirements

- [ ] Requirement 1: 400 status when prompt is empty
  - Acceptance: HTTP status code is 400
  - Acceptance: Error is returned immediately
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates prompt cannot be empty
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: prompt is whitespace only
  - Verify if whitespace-only strings are rejected

### Testing

- [ ] Write test in `tests/tasks-api.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 7: HTTP Server - Tasks API
- **Execution Timing**: Must be completed after TASK-7.4.1
- **Dependencies**: TASK-7.4.1, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - Empty strings should be rejected
  - Validation should be strict
- **Task Independence**: Can be done after TASK-7.4.1

## Related Tasks

- Previous: TASK-7.4.1
- Next: TASK-7.4.3
- Dependencies: TASK-7.4.1, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-7.4: POST /api/tasks (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/tasks-api.test.ts`
2. Test passes
3. Validation is verified (400 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want empty prompts rejected so I don't process invalid requests
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when prompt is empty string
- Test verifies error message is clear
- Test passes
- All tests pass








