### Step 10.3: CursorCLI - PTY vs Spawn
**Objective**: Test PTY usage and fallback.

This task has been broken down into granular subtasks:

1. [TASK-10.3.1](./10.3.1.md): Test PTY is used when node-pty is available
2. [TASK-10.3.2](./10.3.2.md): Test falls back to spawn when PTY fails
3. [TASK-10.3.3](./10.3.3.md): Test logs reflect PTY vs spawn usage

**Note**: Complete all subtasks in order (10.3.1 → 10.3.2 → 10.3.3). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Extend `tests/cursor-cli.test.ts`:
   - Add test: PTY is used when `node-pty` is available
   - Add test: Falls back to spawn when PTY fails
   - Add test: Logs reflect PTY vs spawn usage
   - Mock `node-pty` module availability

**Tests**:
- Verify PTY detection
- Verify fallback behavior
- Verify logging

**Verification**:
- Tests pass
- PTY handling is robust

