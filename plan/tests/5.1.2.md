# TASK-5.1.2: Test GET /api/list returns 500 when service throws

**Section**: 5. HTTP Server - Conversation API
**Subsection**: 5.1.2
**Task ID**: TASK-5.1.2
**Parent Task**: TASK-5.1

## Description

Write a test to verify that `GET /api/list` returns 500 status code when `conversationService.listConversations()` throws an error. This ensures error handling works correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /api/list endpoint, error handling)

## Current State

- GET /api/list endpoint exists
- Error handling exists (or needs to be implemented)
- `tests/server.test.ts` exists
- Test for error handling may not exist yet

**Important**: This test verifies error handling for the list endpoint.

## Checklist

### Preparation and Setup

- [ ] Review GET /api/list endpoint implementation
- [ ] Review error handling logic
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /api/list returns 500 when service throws'`
  - [ ] Use same `describe` block as other /api/list tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Mock `conversationService.listConversations()` to throw an error
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test list endpoint with error
  - [ ] Make GET request to `/api/list`
  - [ ] Verify response status is 500
  - [ ] Verify response body contains error information
- [ ] Step 4: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message is included

### Specific Requirements

- [ ] Requirement 1: Error returns 500 status
  - Acceptance: HTTP status code is 500 when service throws
  - Acceptance: Error is caught and handled
- [ ] Requirement 2: Error response is informative
  - Acceptance: Response contains error information
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining error handling

### Verification

- [ ] Verify test passes
- [ ] Verify error handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 5: HTTP Server - Conversation API
- **Execution Timing**: Must be completed after TASK-5.1.1
- **Dependencies**: TASK-5.1.1, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - Errors should be caught and returned as 500
- **Task Independence**: Can be done after TASK-5.1.1

## Related Tasks

- Previous: TASK-5.1.1
- Next: TASK-5.2.1
- Dependencies: TASK-5.1.1, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-5.1: GET /api/list (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Error handling is verified (500 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want proper error handling so I know when listing fails
- As a **client**, I want error responses so I can handle failures appropriately

**Automated Tests**:
- Test verifies 500 status when service throws
- Test passes
- All tests pass

