# TASK-7.5.3: Test PUT /api/tasks/:id returns 400 when status is not a number

**Section**: 7. HTTP Server - Tasks API
**Subsection**: 7.5.3
**Task ID**: TASK-7.5.3
**Parent Task**: TASK-7.5

## Description

Write a test to verify that `PUT /api/tasks/:id` returns 400 status code when the `status` field is not a number. This ensures type validation works correctly for status updates.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (PUT /api/tasks/:id endpoint, status type validation)

## Current State

- PUT /api/tasks/:id endpoint exists
- Type validation exists (or needs to be implemented)
- `tests/tasks-api.test.ts` exists
- Test for status type validation may not exist yet

**Important**: This test verifies type validation for status field.

## Checklist

### Preparation and Setup

- [ ] Review PUT /api/tasks/:id endpoint implementation
- [ ] Review type validation logic for status parameter
- [ ] Review existing tests in `tests/tasks-api.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'PUT /api/tasks/:id returns 400 when status is not a number'`
  - [ ] Use same `describe` block as other /api/tasks/:id PUT tests
- [ ] Step 2: Set up test data
  - [ ] Create a test task in database for updating
- [ ] Step 3: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 4: Test endpoint with invalid status types
  - [ ] Test case 1: Make PUT request with body: `{ status: 'invalid' }` - verify 400
  - [ ] Test case 2: Make PUT request with body: `{ status: true }` - verify 400
  - [ ] Test case 3: Make PUT request with body: `{ status: {} }` - verify 400
- [ ] Step 5: Verify error responses
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates status must be a number

### Specific Requirements

- [ ] Requirement 1: 400 status for non-number status
  - Acceptance: HTTP status code is 400 for non-number status values
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates status must be a number
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Testing

- [ ] Write test in `tests/tasks-api.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining type validation

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 7: HTTP Server - Tasks API
- **Execution Timing**: Must be completed after TASK-7.5.2
- **Dependencies**: TASK-7.5.2, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - status must be a number type
- **Task Independence**: Can be done after TASK-7.5.2

## Related Tasks

- Previous: TASK-7.5.2
- Next: TASK-7.5.4
- Dependencies: TASK-7.5.2, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-7.5: PUT /api/tasks/:id (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/tasks-api.test.ts`
2. Test passes
3. Type validation is verified (400 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want type validation so I know when status type is invalid
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when status is not a number
- Test passes
- All tests pass












