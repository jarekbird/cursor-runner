# TASK-7.4.5: Test POST /api/tasks creates task with provided order and status

**Section**: 7. HTTP Server - Tasks API
**Subsection**: 7.4.5
**Task ID**: TASK-7.4.5
**Parent Task**: TASK-7.4

## Description

Write a test to verify that `POST /api/tasks` creates a task with provided `order` and `status` values when they are specified in the request body. This ensures custom values work correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /api/tasks endpoint), `src/task-service.ts` (createTask method)

## Current State

- POST /api/tasks endpoint exists
- order and status handling exists
- `tests/tasks-api.test.ts` exists
- Test for provided values may not exist yet

**Important**: This test verifies custom parameter handling.

## Checklist

### Preparation and Setup

- [ ] Review POST /api/tasks endpoint implementation
- [ ] Review TaskService.createTask() method
- [ ] Review existing tests in `tests/tasks-api.test.ts`
- [ ] Use `createMockServer()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /api/tasks creates task with provided order and status'`
  - [ ] Use same `describe` block as other /api/tasks POST tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with order and status
  - [ ] Make POST request to `/api/tasks` with body: `{ prompt: 'test', order: 5, status: 1 }`
  - [ ] Verify response status is 201
  - [ ] Verify task was created in database
  - [ ] Verify created task has order: 5
  - [ ] Verify created task has status: 1
- [ ] Step 4: Verify provided values
  - [ ] Query database to verify task has correct values
  - [ ] Assert order is 5 (not default 0)
  - [ ] Assert status is 1 (not default 0)

### Specific Requirements

- [ ] Requirement 1: Provided order is used
  - Acceptance: Task is created with provided order value
  - Acceptance: Provided value overrides default
- [ ] Requirement 2: Provided status is used
  - Acceptance: Task is created with provided status value
  - Acceptance: Provided value overrides default
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Only order is provided (status should use default)
- [ ] Handle edge case 2: Only status is provided (order should use default)

### Testing

- [ ] Write test in `tests/tasks-api.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Verify provided values are used correctly
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining provided value handling

### Verification

- [ ] Verify test passes
- [ ] Verify provided value handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 7: HTTP Server - Tasks API
- **Execution Timing**: Must be completed after TASK-7.4.4
- **Dependencies**: TASK-7.4.4, TASK-1.1.4
- **Important Considerations**: 
  - Provided values should override defaults
  - Use real database for integration test
- **Task Independence**: Can be done after TASK-7.4.4

## Related Tasks

- Previous: TASK-7.4.4
- Next: TASK-7.4.6
- Dependencies: TASK-7.4.4, TASK-1.1.4
- Related:
  - TASK-7.4: POST /api/tasks (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/tasks-api.test.ts`
2. Test passes
3. Provided values are verified (order and status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want to specify order and status so I can control task properties
- As a **client**, I want to set order and status so I can create tasks with specific values

**Automated Tests**:
- Test verifies provided order is used
- Test verifies provided status is used
- Test passes
- All tests pass

