# TASK-11.1.4: Test ConversationService.getConversationId() returns new UUID when Redis unavailable

**Section**: 11. Redis-based Conversation Services
**Subsection**: 11.1.4
**Task ID**: TASK-11.1.4
**Parent Task**: TASK-11.1

## Description

Write a test to verify that `ConversationService.getConversationId()` returns a new UUID when Redis is unavailable. This ensures graceful fallback behavior when Redis fails.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/conversation-service.ts` (getConversationId method, Redis fallback)

## Current State

- ConversationService.getConversationId() exists
- Redis fallback exists (or needs to be implemented)
- `tests/conversation-service.test.ts` exists
- Test for Redis fallback may not exist yet

**Important**: This test verifies graceful degradation when Redis is unavailable.

## Checklist

### Preparation and Setup

- [ ] Review ConversationService.getConversationId() implementation
- [ ] Review Redis error handling
- [ ] Review existing tests in `tests/conversation-service.test.ts`
- [ ] Use `createMockRedisClient()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'getConversationId() returns new UUID when Redis unavailable'`
  - [ ] Use same `describe` block as other getConversationId tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockRedisClient()` helper
  - [ ] Mock Redis to throw error or return unavailable status
  - [ ] Simulate Redis unavailability
- [ ] Step 3: Test Redis fallback
  - [ ] Call `getConversationId()` with no explicit ID
  - [ ] Verify Redis error is caught
  - [ ] Verify new UUID is returned
  - [ ] Verify UUID format is correct
- [ ] Step 4: Verify fallback behavior
  - [ ] Assert new UUID is returned
  - [ ] Assert UUID format is valid
  - [ ] Assert no exception is thrown

### Specific Requirements

- [ ] Requirement 1: Fallback returns UUID
  - Acceptance: New UUID is returned when Redis unavailable
  - Acceptance: UUID format is valid
- [ ] Requirement 2: Fallback is graceful
  - Acceptance: No exception is thrown
  - Acceptance: System continues to operate
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Redis connection timeout (verify handling)

### Testing

- [ ] Write test in `tests/conversation-service.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining Redis fallback

### Verification

- [ ] Verify test passes
- [ ] Verify fallback works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 11: Redis-based Conversation Services
- **Execution Timing**: Must be completed after TASK-11.1.3
- **Dependencies**: TASK-11.1.3, TASK-1.1.1, TASK-1.1.4
- **Important Considerations**: 
  - Fallback should be graceful
  - UUID should be valid format
- **Task Independence**: Can be done after TASK-11.1.3

## Related Tasks

- Previous: TASK-11.1.3
- Next: TASK-11.2.1
- Dependencies: TASK-11.1.3, TASK-1.1.1, TASK-1.1.4
- Related:
  - TASK-11.1: ConversationService - getConversationId (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/conversation-service.test.ts`
2. Test passes
3. Redis fallback is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want Redis fallback so system works when Redis is down
- As a **system**, I want graceful degradation so I don't crash when Redis fails

**Automated Tests**:
- Test verifies new UUID is returned when Redis unavailable
- Test passes
- All tests pass

