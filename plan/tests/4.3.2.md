# TASK-4.3.2: Test POST /cursor/iterate uses default maxIterations of 5

**Section**: 4. HTTP Server - Cursor Execution Routes
**Subsection**: 4.3.2
**Task ID**: TASK-4.3.2
**Parent Task**: TASK-4.3

## Description

Write a test to verify that `POST /cursor/iterate` uses the default `maxIterations` value of 5 when it is not provided in the request body. This ensures default values work correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /cursor/iterate endpoint, maxIterations default)

## Current State

- POST /cursor/iterate endpoint exists
- maxIterations default handling exists (or needs to be implemented)
- `tests/server.test.ts` exists
- Test for maxIterations default may not exist yet

**Important**: This test verifies default parameter handling.

## Checklist

### Preparation and Setup

- [ ] Review POST /cursor/iterate endpoint implementation
- [ ] Review how maxIterations default is set
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /cursor/iterate uses default maxIterations of 5'`
  - [ ] Use same `describe` block as other cursor/iterate tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Mock `cursorExecution.iterate()` to track maxIterations parameter
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test iterate endpoint without maxIterations
  - [ ] Make POST request to `/cursor/iterate` with body: `{ prompt: 'test' }` (no maxIterations)
  - [ ] Verify iterate() was called with maxIterations: 5
  - [ ] Verify response is successful
- [ ] Step 4: Verify default value
  - [ ] Assert iterate() receives maxIterations: 5
  - [ ] Assert default is used when not provided

### Specific Requirements

- [ ] Requirement 1: Default maxIterations is 5
  - Acceptance: iterate() is called with maxIterations: 5 when not provided
  - Acceptance: Default value is correct
- [ ] Requirement 2: Default is used correctly
  - Acceptance: Default is applied when maxIterations is missing
  - Acceptance: Default is not used when maxIterations is provided
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests
  - Acceptance: Mocks are properly cleaned up

### Error Handling and Edge Cases

- [ ] Handle edge case 1: maxIterations is 0
  - Verify 0 is treated as valid (not default)
- [ ] Handle edge case 2: maxIterations is negative
  - Verify validation (may be separate test)

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Verify default maxIterations is used correctly
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining default value
- [ ] Document default maxIterations value

### Verification

- [ ] Verify test passes
- [ ] Verify default handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 4: HTTP Server - Cursor Execution Routes
- **Execution Timing**: Must be completed after TASK-4.3.1
- **Dependencies**: TASK-4.3.1, TASK-1.1.4
- **Important Considerations**: 
  - Default should be 5 as specified
  - Default should only apply when maxIterations is not provided
- **Task Independence**: Can be done after TASK-4.3.1

## Related Tasks

- Previous: TASK-4.3.1
- Next: TASK-4.3.3
- Dependencies: TASK-4.3.1, TASK-1.1.4
- Related:
  - TASK-4.3: POST /cursor/iterate (Synchronous) (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Default maxIterations is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want default maxIterations so I don't have to specify it every time
- As a **client**, I want sensible defaults so requests are simpler

**Automated Tests**:
- Test verifies default maxIterations of 5 is used
- Test passes
- All tests pass

