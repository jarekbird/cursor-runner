# TASK-5.3.1: Test GET /api/working-directory/files returns 500 when TARGET_APP_PATH not set

**Section**: 5. HTTP Server - Conversation API
**Subsection**: 5.3.1
**Task ID**: TASK-5.3.1
**Parent Task**: TASK-5.3

## Description

Write a test to verify that `GET /api/working-directory/files` returns 500 status code when `TARGET_APP_PATH` environment variable is not set. This ensures proper error handling for missing configuration.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /api/working-directory/files endpoint, TARGET_APP_PATH validation)

## Current State

- GET /api/working-directory/files endpoint exists
- TARGET_APP_PATH validation exists (or needs to be implemented)
- `tests/server.test.ts` exists
- Test for TARGET_APP_PATH validation may not exist yet

**Important**: This test verifies configuration validation.

## Checklist

### Preparation and Setup

- [ ] Review GET /api/working-directory/files endpoint implementation
- [ ] Review how TARGET_APP_PATH is accessed
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /api/working-directory/files returns 500 when TARGET_APP_PATH not set'`
  - [ ] Use `describe` block for /api/working-directory/files endpoint tests
- [ ] Step 2: Set up test environment
  - [ ] Use `beforeEach` to save original TARGET_APP_PATH value
  - [ ] Delete `TARGET_APP_PATH` from `process.env`
  - [ ] Use `afterEach` to restore original env var
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint without TARGET_APP_PATH
  - [ ] Make GET request to `/api/working-directory/files`
  - [ ] Verify response status is 500
  - [ ] Verify response body contains error information
- [ ] Step 4: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message mentions TARGET_APP_PATH

### Specific Requirements

- [ ] Requirement 1: Error returns 500 status
  - Acceptance: HTTP status code is 500 when TARGET_APP_PATH is not set
  - Acceptance: Error is caught and handled
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates TARGET_APP_PATH is missing
  - Acceptance: Error message is actionable
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests
  - Acceptance: Env var is restored after test

### Error Handling and Edge Cases

- [ ] Handle edge case 1: TARGET_APP_PATH is empty string
  - Verify empty string is treated as missing

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation
- [ ] Document required environment variables

### Verification

- [ ] Verify test passes
- [ ] Verify error handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 5: HTTP Server - Conversation API
- **Execution Timing**: Must be completed after TASK-5.2.3
- **Dependencies**: TASK-5.2.3, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - TARGET_APP_PATH is required for file tree operations
  - Must properly clean up env vars
- **Task Independence**: Can be done after TASK-5.2.3

## Related Tasks

- Previous: TASK-5.2.3
- Next: TASK-5.3.2
- Dependencies: TASK-5.2.3, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-5.3: GET /api/working-directory/files (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Error handling is verified (500 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want proper error handling so I know when TARGET_APP_PATH is missing
- As a **client**, I want error responses so I can handle configuration issues

**Automated Tests**:
- Test verifies 500 status when TARGET_APP_PATH is not set
- Test verifies error message is clear
- Test passes
- All tests pass








