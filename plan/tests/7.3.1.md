# TASK-7.3.1: Test GET /api/tasks/:id returns 400 when id is not numeric

**Section**: 7. HTTP Server - Tasks API
**Subsection**: 7.3.1
**Task ID**: TASK-7.3.1
**Parent Task**: TASK-7.3

## Description

Write a test to verify that `GET /api/tasks/:id` returns 400 status code when the id parameter is not numeric. This ensures proper validation for task ID parameter.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /api/tasks/:id endpoint, id validation)

## Current State

- GET /api/tasks/:id endpoint exists
- Validation logic exists (or needs to be implemented)
- `tests/tasks-api.test.ts` exists
- Test for id validation may not exist yet

**Important**: This test verifies ID parameter validation.

## Checklist

### Preparation and Setup

- [ ] Review GET /api/tasks/:id endpoint implementation
- [ ] Review validation logic for id parameter
- [ ] Review existing tests in `tests/tasks-api.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /api/tasks/:id returns 400 when id is not numeric'`
  - [ ] Use `describe` block for /api/tasks/:id endpoint tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with invalid id values
  - [ ] Test case 1: Make GET request to `/api/tasks/abc` - verify 400
  - [ ] Test case 2: Make GET request to `/api/tasks/invalid` - verify 400
  - [ ] Test case 3: Make GET request to `/api/tasks/12.5` - verify 400 (if decimals are invalid)
- [ ] Step 4: Verify error responses
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates id must be numeric

### Specific Requirements

- [ ] Requirement 1: 400 status for non-numeric id
  - Acceptance: HTTP status code is 400 for non-numeric id values
  - Acceptance: Error is returned immediately
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates id must be numeric
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: id is empty string
  - Verify empty string is treated as invalid
- [ ] Handle edge case 2: id is valid number as string ('1', '123')
  - Verify these are accepted (not invalid)

### Testing

- [ ] Write test in `tests/tasks-api.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation
- [ ] Document valid id format

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 7: HTTP Server - Tasks API
- **Execution Timing**: Must be completed after TASK-7.2.4
- **Dependencies**: TASK-7.2.4, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - Task ID must be numeric (integer)
  - Validation should happen before database query
- **Task Independence**: Can be done after TASK-7.2.4

## Related Tasks

- Previous: TASK-7.2.4
- Next: TASK-7.3.2
- Dependencies: TASK-7.2.4, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-7.3: GET /api/tasks/:id (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/tasks-api.test.ts`
2. Test passes
3. Validation is verified (400 status for non-numeric id)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want validation errors so I know when task ID is invalid
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when id is not numeric
- Test verifies error message is clear
- Test passes
- All tests pass












