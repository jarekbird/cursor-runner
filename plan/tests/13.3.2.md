# TASK-13.3.2: Test isElevenLabsEnabled() logs warning for invalid values

**Section**: 13. System Settings, Gmail MCP, Feature Flags
**Subsection**: 13.3.2
**Task ID**: TASK-13.3.2
**Parent Task**: TASK-13.3

## Description

Write a test to verify that `isElevenLabsEnabled()` logs a warning when an invalid value is provided. This ensures invalid configurations are detected and logged.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/feature-flags.ts` (isElevenLabsEnabled method, invalid value handling)

## Current State

- isElevenLabsEnabled() exists
- Invalid value handling exists
- `tests/feature-flags.test.ts` exists
- Test for invalid value logging may not exist yet

**Important**: This test verifies invalid value detection and logging.

## Checklist

### Preparation and Setup

- [ ] Review isElevenLabsEnabled() implementation
- [ ] Review invalid value detection logic
- [ ] Review existing tests in `tests/feature-flags.test.ts`
- [ ] Use test environment setup and logger mocking

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'isElevenLabsEnabled() logs warning for invalid values'`
  - [ ] Use same `describe` block as other ElevenLabs tests
- [ ] Step 2: Set up test environment
  - [ ] Use `beforeEach` to save original ENABLE_ELEVENLABS env var
  - [ ] Set up logger spy to capture log messages
  - [ ] Use `afterEach` to restore original env var
- [ ] Step 3: Test isElevenLabsEnabled() with invalid value
  - [ ] Set `ENABLE_ELEVENLABS='invalid'` (not true/false/1/0)
  - [ ] Call `isElevenLabsEnabled()`
  - [ ] Verify warning is logged
  - [ ] Verify warning message indicates invalid value
- [ ] Step 4: Verify logging
  - [ ] Assert warning is logged
  - [ ] Assert log message is clear

### Specific Requirements

- [ ] Requirement 1: Warning is logged
  - Acceptance: Logger is called with warning level
  - Acceptance: Warning message is clear
- [ ] Requirement 2: Invalid value is detected
  - Acceptance: Invalid values trigger warning
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Empty string (verify if treated as invalid)

### Testing

- [ ] Write test in `tests/feature-flags.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining invalid value handling

### Verification

- [ ] Verify test passes
- [ ] Verify warning logging works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 13: System Settings, Gmail MCP, Feature Flags
- **Execution Timing**: Must be completed after TASK-13.3.1
- **Dependencies**: TASK-13.3.1
- **Important Considerations**: 
  - Invalid values should be logged but not crash
  - Warning should be helpful
- **Task Independence**: Can be done after TASK-13.3.1

## Related Tasks

- Previous: TASK-13.3.1
- Next: TASK-13.3.3
- Dependencies: TASK-13.3.1
- Related:
  - TASK-13.3: ElevenLabs Feature Flags (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/feature-flags.test.ts`
2. Test passes
3. Warning logging is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want warnings for invalid values so I can fix configuration
- As a **system**, I want invalid value detection so configuration errors are caught

**Automated Tests**:
- Test verifies warning is logged for invalid values
- Test passes
- All tests pass

