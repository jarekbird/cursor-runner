# TASK-13.2.2: Test getGmailMcpEnabled() returns true for various true values

**Section**: 13. System Settings, Gmail MCP, Feature Flags
**Subsection**: 13.2.2
**Task ID**: TASK-13.2.2
**Parent Task**: TASK-13.2

## Description

Write a test to verify that `getGmailMcpEnabled()` returns true for various true values of the `ENABLE_GMAIL_MCP` environment variable (e.g., 'true', '1', 'yes', etc.). This ensures flexible enablement works correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/system-settings-gmail-validation.ts` (getGmailMcpEnabled method, true value parsing)

## Current State

- getGmailMcpEnabled() exists
- True value parsing exists
- `tests/system-settings-gmail-validation.test.ts` exists
- Test for various true values may not exist yet

**Important**: This test verifies flexible enablement.

## Checklist

### Preparation and Setup

- [ ] Review getGmailMcpEnabled() implementation
- [ ] Review true value parsing logic
- [ ] Review existing tests in `tests/system-settings-gmail-validation.test.ts`
- [ ] Use test environment setup

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'getGmailMcpEnabled() returns true for various true values'`
  - [ ] Use same `describe` block as other Gmail MCP tests
- [ ] Step 2: Set up test environment
  - [ ] Use `beforeEach` to save original ENABLE_GMAIL_MCP env var
  - [ ] Use `afterEach` to restore original env var
- [ ] Step 3: Test getGmailMcpEnabled() with various true values
  - [ ] Test case 1: Set `ENABLE_GMAIL_MCP='true'` - verify true
  - [ ] Test case 2: Set `ENABLE_GMAIL_MCP='1'` - verify true
  - [ ] Test case 3: Set `ENABLE_GMAIL_MCP='yes'` - verify true (if supported)
  - [ ] Test case 4: Set `ENABLE_GMAIL_MCP='TRUE'` - verify true (if case-insensitive)
- [ ] Step 4: Verify true value parsing
  - [ ] Assert all true variants return true
  - [ ] Assert parsing is correct

### Specific Requirements

- [ ] Requirement 1: True values are recognized
  - Acceptance: Various true values return true
  - Acceptance: Parsing is flexible
- [ ] Requirement 2: Parsing is consistent
  - Acceptance: All true variants work
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Case sensitivity (verify expected behavior)

### Testing

- [ ] Write test in `tests/system-settings-gmail-validation.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining true value parsing

### Verification

- [ ] Verify test passes
- [ ] Verify true value parsing works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 13: System Settings, Gmail MCP, Feature Flags
- **Execution Timing**: Must be completed after TASK-13.2.1
- **Dependencies**: TASK-13.2.1
- **Important Considerations**: 
  - Should support common true value formats
  - Parsing should be consistent
- **Task Independence**: Can be done after TASK-13.2.1

## Related Tasks

- Previous: TASK-13.2.1
- Next: TASK-13.2.3
- Dependencies: TASK-13.2.1
- Related:
  - TASK-13.2: Gmail MCP Configuration (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/system-settings-gmail-validation.test.ts`
2. Test passes
3. True value parsing is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want flexible true values so configuration is easier
- As a **client**, I want various formats so I can use different conventions

**Automated Tests**:
- Test verifies various true values return true
- Test passes
- All tests pass

