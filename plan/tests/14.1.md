### Step 14.1: GitService
**Objective**: Test GitService behavior.

This task has been broken down into granular subtasks:

1. [TASK-14.1.1](./14.1.1.md): Test repositoriesPath uses env var when set
2. [TASK-14.1.2](./14.1.2.md): Test repositoriesPath uses default when env not set
3. [TASK-14.1.3](./14.1.3.md): Test ensureRepositoriesDirectory() creates directory when missing
4. [TASK-14.1.4](./14.1.4.md): Test ensureRepositoriesDirectory() is idempotent

**Note**: Complete all subtasks in order (14.1.1 → 14.1.2 → 14.1.3 → 14.1.4). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Extend `tests/git-service.test.ts`:
   - Add test: `repositoriesPath` uses env var when set
   - Add test: `repositoriesPath` uses default when env not set
   - Add test: `ensureRepositoriesDirectory()` creates directory when missing
   - Add test: `ensureRepositoriesDirectory()` is idempotent
   - Mock `fs.existsSync` and `fs.mkdirSync`

**Tests**:
- Verify path resolution
- Verify directory creation
- Verify idempotency

**Verification**:
- Tests pass

