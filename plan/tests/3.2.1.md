# TASK-3.2.1: Test GET /health/queue returns queue status

**Section**: 3. HTTP Server - Health & Diagnostics
**Subsection**: 3.2.1
**Task ID**: TASK-3.2.1
**Parent Task**: TASK-3.2

## Description

Write a test to verify that `GET /health/queue` returns queue status from `cursorCLI.getQueueStatus()`. This ensures the queue diagnostic endpoint works correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (health/queue endpoint), `src/cursor-cli.ts` (getQueueStatus method)

## Current State

- Health/queue endpoint exists in server
- `cursorCLI.getQueueStatus()` exists
- `tests/server.test.ts` exists
- Test for queue health endpoint may not exist yet

**Important**: This test verifies the queue status endpoint works correctly.

## Checklist

### Preparation and Setup

- [ ] Review health/queue endpoint implementation
- [ ] Review `cursorCLI.getQueueStatus()` method
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` and `createMockCursorCLI()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /health/queue returns queue status'`
  - [ ] Use `describe` block for health/queue endpoint tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `createMockCursorCLI()` helper or mock getQueueStatus()
  - [ ] Mock `getQueueStatus()` to return test data: `{ available: 5, waiting: 0, maxConcurrent: 5 }`
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test queue endpoint
  - [ ] Make GET request to `/health/queue`
  - [ ] Verify response status is 200
  - [ ] Verify response body contains queue status
  - [ ] Verify response includes `available`, `waiting`, `maxConcurrent` fields
- [ ] Step 4: Verify response structure
  - [ ] Use `assertSuccessResponse()` helper
  - [ ] Verify response matches queue status structure
  - [ ] Verify values match mocked getQueueStatus() return value

### Specific Requirements

- [ ] Requirement 1: Response contains queue status
  - Acceptance: Response includes queue status from getQueueStatus()
  - Acceptance: All queue status fields are present
- [ ] Requirement 2: Response status is 200
  - Acceptance: HTTP status code is 200
  - Acceptance: No errors are thrown
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests
  - Acceptance: Mocks are properly cleaned up

### Error Handling and Edge Cases

- [ ] Handle edge case 1: getQueueStatus() throws
  - Verify error handling (may be separate test)

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Verify queue status is returned correctly
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining what is being tested
- [ ] Document expected response format

### Verification

- [ ] Verify test passes
- [ ] Verify response matches expected contract
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 3: HTTP Server - Health & Diagnostics
- **Execution Timing**: Must be completed after TASK-3.1.2
- **Dependencies**: TASK-3.1.2, TASK-1.1.3, TASK-1.1.4, TASK-1.1.6
- **Important Considerations**: 
  - Use test helpers from Phase 1.1
  - Mock getQueueStatus() to return predictable values
- **Task Independence**: Can be done after TASK-3.1.2

## Related Tasks

- Previous: TASK-3.1.2
- Next: TASK-3.2.2
- Dependencies: TASK-3.1.2, TASK-1.1.3, TASK-1.1.4, TASK-1.1.6
- Related:
  - TASK-3.2: GET /health/queue Endpoint (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Queue status is verified in response
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want a queue status endpoint so I can monitor cursor CLI queue health
- As a **monitoring system**, I want queue status so I can alert when queue is full

**Automated Tests**:
- Test verifies GET /health/queue returns queue status
- Test verifies response contains all queue status fields
- Test passes
- All tests pass












