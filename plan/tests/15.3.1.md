# TASK-15.3.1: Test agent conversation E2E flow POST /api/agent/new → POST /api/agent/:id/message → GET /api/agent/:id

**Section**: 15. E2E-lite Flows
**Subsection**: 15.3.1
**Task ID**: TASK-15.3.1
**Parent Task**: TASK-15.3

## Description

Write an E2E-lite test to verify the agent conversation workflow: create agent conversation, add message, retrieve conversation. This ensures the complete agent conversation flow works end-to-end.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /api/agent/new, POST /api/agent/:id/message, GET /api/agent/:id endpoints)

## Current State

- Agent conversation API endpoints exist
- AgentConversationService exists
- `tests/e2e/agent-conversation.test.ts` exists (or needs to be created)
- E2E-lite test for agent conversation flow may not exist yet

**Important**: This test verifies complete agent conversation flow.

## Checklist

### Preparation and Setup

- [ ] Review agent conversation API endpoints
- [ ] Review AgentConversationService methods
- [ ] Review existing E2E test patterns
- [ ] Use `createMockServer()` and `createMockRedisClient()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Create `tests/e2e/agent-conversation.test.ts` if it doesn't exist
  - [ ] Add test: `'POST /api/agent/new → POST /api/agent/:id/message → GET /api/agent/:id'`
  - [ ] Use `describe` block for agent conversation E2E tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `createMockRedisClient()` helper for agent conversation storage
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test agent conversation flow
  - [ ] Step 3.1: POST to `/api/agent/new` - verify conversation ID is returned
  - [ ] Step 3.2: POST to `/api/agent/{id}/message` with message - verify success
  - [ ] Step 3.3: GET `/api/agent/{id}` - verify conversation includes message
  - [ ] Verify conversation ID is consistent across all requests
- [ ] Step 4: Verify agent conversation flow
  - [ ] Assert conversation ID is returned and consistent
  - [ ] Assert message is added to conversation
  - [ ] Assert conversation retrieval works

### Specific Requirements

- [ ] Requirement 1: Agent conversation flow works
  - Acceptance: Create → Message → Retrieve flow completes
  - Acceptance: All steps execute correctly
- [ ] Requirement 2: Conversation state is consistent
  - Acceptance: Conversation ID is consistent
  - Acceptance: Messages are stored correctly
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Agent conversation with metadata (verify handling)

### Testing

- [ ] Write test in `tests/e2e/agent-conversation.test.ts`
- [ ] Run test: `npm run test:e2e`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining agent conversation flow

### Verification

- [ ] Verify test passes
- [ ] Verify agent conversation flow works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 15: E2E-lite Flows
- **Execution Timing**: Must be completed after TASK-15.2.1
- **Dependencies**: TASK-15.2.1, TASK-1.1.1, TASK-1.1.4
- **Important Considerations**: 
  - Should use mocked Redis (no real Redis)
  - Should verify complete flow
- **Task Independence**: Can be done after TASK-15.2.1

## Related Tasks

- Previous: TASK-15.2.1
- Next: TASK-16.1.1
- Dependencies: TASK-15.2.1, TASK-1.1.1, TASK-1.1.4
- Related:
  - TASK-15.3: Agent Conversation E2E Flow (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/e2e/agent-conversation.test.ts`
2. Test passes
3. Agent conversation flow is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want E2E tests so I can verify agent conversation workflows
- As a **client**, I want verified flows so agent conversations work correctly

**Automated Tests**:
- Test verifies complete agent conversation flow
- Test passes
- All tests pass

