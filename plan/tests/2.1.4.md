# TASK-2.1.4: Test validateConfig handles multiple missing env vars

**Section**: 2. Core Application Lifecycle
**Subsection**: 2.1.4
**Task ID**: TASK-2.1.4
**Parent Task**: TASK-2.1

## Description

Write a test to verify that `validateConfig()` properly handles and reports multiple missing environment variables. This ensures comprehensive configuration validation and helpful error messages.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/index.ts` (validateConfig method)

## Current State

- `validateConfig()` method exists
- `tests/index.test.ts` exists
- Tests for single missing env var exist (TASK-2.1.2)
- Test for multiple missing vars may not exist yet

**Important**: This test ensures error messages are comprehensive when multiple config issues exist.

## Checklist

### Preparation and Setup

- [ ] Review `validateConfig()` implementation
- [ ] Identify all required environment variables
- [ ] Review existing tests in `tests/index.test.ts`
- [ ] Understand how multiple errors are reported

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'validateConfig handles multiple missing env vars'`
  - [ ] Use same `describe` block as other validateConfig tests
- [ ] Step 2: Set up test environment
  - [ ] Use `beforeEach` to save original env var values
  - [ ] Delete multiple required env vars (e.g., CURSOR_CLI_PATH, REDIS_URL, etc.)
  - [ ] Use `afterEach` to restore original env vars
- [ ] Step 3: Test validation
  - [ ] Create CursorRunner instance (or call validateConfig directly)
  - [ ] Verify error is thrown
  - [ ] Verify error message lists all missing env vars
- [ ] Step 4: Verify error message
  - [ ] Assert error message mentions all missing vars
  - [ ] Assert error message format is clear and actionable
  - [ ] Assert error message helps identify all issues at once

### Specific Requirements

- [ ] Requirement 1: Error is thrown when multiple vars are missing
  - Acceptance: Test fails if error is not thrown
  - Acceptance: Error is thrown synchronously or promise is rejected
- [ ] Requirement 2: Error message lists all missing vars
  - Acceptance: Error message includes all missing environment variables
  - Acceptance: Error message format is clear and readable
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests
  - Acceptance: Env vars are restored after test

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Some vars missing, some present
  - Verify error only lists missing vars
- [ ] Handle edge case 2: All required vars missing
  - Verify comprehensive error message

### Testing

- [ ] Write test in `tests/index.test.ts`
- [ ] Run test: `npm run test:unit`
- [ ] Verify test passes
- [ ] Verify error message includes all missing vars
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining what is being tested
- [ ] Document expected error message format for multiple vars

### Verification

- [ ] Verify test passes
- [ ] Verify error message is comprehensive
- [ ] Verify no regressions in existing tests
- [ ] Verify test is maintainable

## Notes

- This task is part of Phase 2: Core Application Lifecycle
- **Execution Timing**: Must be completed after TASK-2.1.3
- **Dependencies**: TASK-2.1.3
- **Important Considerations**: 
  - Must properly clean up all env vars
  - Error message should be comprehensive but readable
- **Task Independence**: Can be done after TASK-2.1.3

## Related Tasks

- Previous: TASK-2.1.3
- Next: TASK-2.2.1
- Dependencies: TASK-2.1.3
- Related:
  - TASK-2.1: CursorRunner Constructor & Config Validation (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/index.test.ts`
2. Test passes
3. Error message is verified to list all missing vars
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want to see all missing config vars at once so I can fix all issues in one go
- As a **system**, I want comprehensive validation so I can identify all configuration problems before starting

**Automated Tests**:
- Test verifies error is thrown when multiple vars are missing
- Test verifies error message lists all missing vars
- Test passes
- All tests pass












