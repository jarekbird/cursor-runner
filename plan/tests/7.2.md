### Step 7.2: GET /api/tasks
**Objective**: Test tasks listing endpoint.

This task has been broken down into granular subtasks:

1. [TASK-7.2.1](./7.2.1.md): Test GET /api/tasks returns all tasks when no filter
2. [TASK-7.2.2](./7.2.2.md): Test GET /api/tasks?status=0 returns only ready tasks
3. [TASK-7.2.3](./7.2.3.md): Test GET /api/tasks?status=1 returns only complete tasks
4. [TASK-7.2.4](./7.2.4.md): Test GET /api/tasks?status=invalid returns 400

**Note**: Complete all subtasks in order (7.2.1 → 7.2.2 → 7.2.3 → 7.2.4). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Extend `tests/tasks-api.test.ts`:
   - Add test: `GET /api/tasks` returns all tasks when no filter
   - Add test: `GET /api/tasks?status=0` returns only ready tasks
   - Add test: `GET /api/tasks?status=1` returns only complete tasks
   - Add test: `GET /api/tasks?status=invalid` returns 400
   - Create test tasks with different statuses in `beforeEach`

**Tests**:
- Verify filtering works
- Verify validation
- Verify response includes `status_label`

**Verification**:
- Tests pass
- Filtering is accurate

