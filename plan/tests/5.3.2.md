# TASK-5.3.2: Test GET /api/working-directory/files returns 404 when directory doesn't exist

**Section**: 5. HTTP Server - Conversation API
**Subsection**: 5.3.2
**Task ID**: TASK-5.3.2
**Parent Task**: TASK-5.3

## Description

Write a test to verify that `GET /api/working-directory/files` returns 404 status code when the directory specified by `TARGET_APP_PATH` doesn't exist. This ensures proper error handling for missing directories.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /api/working-directory/files endpoint), `src/filesystem-service.ts` (exists method)

## Current State

- GET /api/working-directory/files endpoint exists
- FilesystemService.exists() exists
- `tests/server.test.ts` exists
- Test for directory existence check may not exist yet

**Important**: This test verifies directory existence validation.

## Checklist

### Preparation and Setup

- [ ] Review GET /api/working-directory/files endpoint implementation
- [ ] Review FilesystemService.exists() method
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /api/working-directory/files returns 404 when directory doesn't exist'`
  - [ ] Use same `describe` block as other /api/working-directory/files tests
- [ ] Step 2: Set up test environment
  - [ ] Use `beforeEach` to save original TARGET_APP_PATH value
  - [ ] Set `TARGET_APP_PATH` to a non-existent directory path
  - [ ] Use `afterEach` to restore original env var
  - [ ] Use `createMockServer()` helper
  - [ ] Mock `filesystem.exists()` to return false
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with non-existent directory
  - [ ] Make GET request to `/api/working-directory/files`
  - [ ] Verify response status is 404
  - [ ] Verify response body contains error information
- [ ] Step 4: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates directory doesn't exist

### Specific Requirements

- [ ] Requirement 1: Error returns 404 status
  - Acceptance: HTTP status code is 404 when directory doesn't exist
  - Acceptance: Error is caught and handled
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates directory doesn't exist
  - Acceptance: Error message is actionable
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests
  - Acceptance: Env var is restored after test

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Directory path is invalid format
  - Verify validation (if applicable)

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining directory validation

### Verification

- [ ] Verify test passes
- [ ] Verify error handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 5: HTTP Server - Conversation API
- **Execution Timing**: Must be completed after TASK-5.3.1
- **Dependencies**: TASK-5.3.1, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - 404 is appropriate for missing directory
  - Must properly clean up env vars
- **Task Independence**: Can be done after TASK-5.3.1

## Related Tasks

- Previous: TASK-5.3.1
- Next: TASK-5.3.3
- Dependencies: TASK-5.3.1, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-5.3: GET /api/working-directory/files (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Error handling is verified (404 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want proper error handling so I know when directory doesn't exist
- As a **client**, I want 404 responses so I can handle missing directories

**Automated Tests**:
- Test verifies 404 status when directory doesn't exist
- Test verifies error message is clear
- Test passes
- All tests pass

