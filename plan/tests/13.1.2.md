# TASK-13.1.2: Test isSystemSettingEnabled() falls back to env for debug when DB unavailable

**Section**: 13. System Settings, Gmail MCP, Feature Flags
**Subsection**: 13.1.2
**Task ID**: TASK-13.1.2
**Parent Task**: TASK-13.1

## Description

Write a test to verify that `isSystemSettingEnabled()` falls back to environment variable for `debug` setting when the database is unavailable. This ensures fallback behavior works correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/system-settings.ts` (isSystemSettingEnabled method, env fallback)

## Current State

- SystemSettings.isSystemSettingEnabled() exists
- Environment variable fallback exists
- `tests/system-settings.test.ts` exists
- Test for env fallback may not exist yet

**Important**: This test verifies fallback behavior.

## Checklist

### Preparation and Setup

- [ ] Review SystemSettings.isSystemSettingEnabled() implementation
- [ ] Review environment variable fallback logic
- [ ] Review existing tests in `tests/system-settings.test.ts`
- [ ] Use `createTempSqliteDb()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'isSystemSettingEnabled() falls back to env for debug when DB unavailable'`
  - [ ] Use same `describe` block as other system settings tests
- [ ] Step 2: Set up test environment
  - [ ] Use `beforeEach` to save original DEBUG env var
  - [ ] Set `process.env.DEBUG` to 'true'
  - [ ] Use `afterEach` to restore original env var
  - [ ] Mock database to be unavailable (throw error or return null)
- [ ] Step 3: Test isSystemSettingEnabled() with DB unavailable
  - [ ] Call `isSystemSettingEnabled('debug')` with DB unavailable
  - [ ] Verify value is read from environment variable
  - [ ] Verify returned value matches env var value
- [ ] Step 4: Verify fallback behavior
  - [ ] Assert value is read from env var
  - [ ] Assert value matches env var

### Specific Requirements

- [ ] Requirement 1: Fallback to env var
  - Acceptance: Environment variable is read when DB unavailable
  - Acceptance: Value from env var is returned
- [ ] Requirement 2: Fallback is correct
  - Acceptance: Returned value matches env var value
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests
  - Acceptance: Env var is restored after test

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Env var not set (verify returns false)

### Testing

- [ ] Write test in `tests/system-settings.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining fallback behavior

### Verification

- [ ] Verify test passes
- [ ] Verify fallback behavior works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 13: System Settings, Gmail MCP, Feature Flags
- **Execution Timing**: Must be completed after TASK-13.1.1
- **Dependencies**: TASK-13.1.1, TASK-1.1.2
- **Important Considerations**: 
  - Fallback should only work for 'debug' setting
  - Env var should be cleaned up after test
- **Task Independence**: Can be done after TASK-13.1.1

## Related Tasks

- Previous: TASK-13.1.1
- Next: TASK-13.1.3
- Dependencies: TASK-13.1.1, TASK-1.1.2
- Related:
  - TASK-13.1: System Settings - Database & Env Fallback (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/system-settings.test.ts`
2. Test passes
3. Fallback behavior is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want env fallback so debug works even when DB is unavailable
- As a **system**, I want fallback so critical settings are always available

**Automated Tests**:
- Test verifies fallback to env var when DB unavailable
- Test passes
- All tests pass
