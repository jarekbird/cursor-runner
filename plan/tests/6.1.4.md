# TASK-6.1.4: Test GET /api/agent/list returns 400 when sortOrder is invalid

**Section**: 6. HTTP Server - Agent Conversation API
**Subsection**: 6.1.4
**Task ID**: TASK-6.1.4
**Parent Task**: TASK-6.1

## Description

Write a test to verify that `GET /api/agent/list` returns 400 status code when the `sortOrder` query parameter is invalid (not 'asc' or 'desc'). This ensures proper validation for sorting order parameters.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /api/agent/list endpoint, sortOrder validation)

## Current State

- GET /api/agent/list endpoint exists
- Validation logic exists (or needs to be implemented)
- `tests/agent-conversation-api.integration.test.ts` exists
- Test for sortOrder validation may not exist yet

**Important**: This test verifies sorting order parameter validation.

## Checklist

### Preparation and Setup

- [ ] Review GET /api/agent/list endpoint implementation
- [ ] Review validation logic for sortOrder parameter
- [ ] Review existing tests in `tests/agent-conversation-api.integration.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /api/agent/list returns 400 when sortOrder is invalid'`
  - [ ] Use same `describe` block as other /api/agent/list tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with invalid sortOrder values
  - [ ] Test case 1: Make GET request to `/api/agent/list?sortOrder=invalid` - verify 400
  - [ ] Test case 2: Make GET request to `/api/agent/list?sortOrder=ASC` - verify 400 (if case-sensitive)
  - [ ] Test case 3: Make GET request to `/api/agent/list?sortOrder=DESC` - verify 400 (if case-sensitive)
  - [ ] Test case 4: Make GET request to `/api/agent/list?sortOrder=random` - verify 400
- [ ] Step 4: Verify error responses
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates sortOrder is invalid
  - [ ] Verify error message indicates allowed values ('asc' or 'desc')

### Specific Requirements

- [ ] Requirement 1: 400 status for invalid sortOrder
  - Acceptance: HTTP status code is 400 for values not 'asc' or 'desc'
  - Acceptance: Error is returned immediately
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates sortOrder is invalid
  - Acceptance: Error message indicates allowed values
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: sortOrder is undefined (should use default, not error)
- [ ] Handle edge case 2: Case sensitivity (verify if 'ASC'/'DESC' are accepted or only 'asc'/'desc')

### Testing

- [ ] Write test in `tests/agent-conversation-api.integration.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation
- [ ] Document allowed sortOrder values

### Verification

- [ ] Verify test passes
- [ ] Verify validation is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 6: HTTP Server - Agent Conversation API
- **Execution Timing**: Must be completed after TASK-6.1.3
- **Dependencies**: TASK-6.1.3, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - sortOrder must be 'asc' or 'desc'
  - Case sensitivity should be consistent
- **Task Independence**: Can be done after TASK-6.1.3

## Related Tasks

- Previous: TASK-6.1.3
- Next: TASK-6.1.5
- Dependencies: TASK-6.1.3, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-6.1: GET /api/agent/list (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/agent-conversation-api.integration.test.ts`
2. Test passes
3. Validation is verified (400 status for invalid sortOrder values)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want validation errors so I know when sorting parameters are invalid
- As a **client**, I want clear error messages so I can fix invalid requests

**Automated Tests**:
- Test verifies 400 status when sortOrder is invalid
- Test verifies error message indicates allowed values
- Test passes
- All tests pass












