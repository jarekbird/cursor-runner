### Step 12.1: TaskService - Basic Operations
**Objective**: Test TaskService CRUD.

This task has been broken down into granular subtasks:

1. [TASK-12.1.1](./12.1.1.md): Test listTasks() returns all tasks ordered by order then id
2. [TASK-12.1.2](./12.1.2.md): Test listTasks(status) filters by status
3. [TASK-12.1.3](./12.1.3.md): Test createTask() creates with timestamps and correct status_label
4. [TASK-12.1.4](./12.1.4.md): Test getTaskById() returns task or null

**Note**: Complete all subtasks in order (12.1.1 → 12.1.2 → 12.1.3 → 12.1.4). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Create `tests/task-service.test.ts`:
   - Add test: `listTasks()` returns all tasks ordered by `order` then `id`
   - Add test: `listTasks(status)` filters by status
   - Add test: `createTask()` creates with timestamps and correct `status_label`
   - Add test: `getTaskById()` returns task or null
   - Use temp SQLite DB with migrations

**Tests**:
- Verify CRUD operations
- Verify ordering
- Verify status labels

**Verification**:
- Tests pass

