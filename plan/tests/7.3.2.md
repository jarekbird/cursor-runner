# TASK-7.3.2: Test GET /api/tasks/:id returns 404 when task not found

**Section**: 7. HTTP Server - Tasks API
**Subsection**: 7.3.2
**Task ID**: TASK-7.3.2
**Parent Task**: TASK-7.3

## Description

Write a test to verify that `GET /api/tasks/:id` returns 404 status code when the task is not found in the database. This ensures proper error handling for non-existent tasks.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /api/tasks/:id endpoint), `src/task-service.ts` (getTaskById method)

## Current State

- GET /api/tasks/:id endpoint exists
- TaskService.getTaskById() exists
- `tests/tasks-api.test.ts` exists
- Test for 404 handling may not exist yet

**Important**: This test verifies 404 error handling.

## Checklist

### Preparation and Setup

- [ ] Review GET /api/tasks/:id endpoint implementation
- [ ] Review TaskService.getTaskById() method
- [ ] Review existing tests in `tests/tasks-api.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /api/tasks/:id returns 404 when task not found'`
  - [ ] Use same `describe` block as other /api/tasks/:id tests
- [ ] Step 2: Set up test data
  - [ ] Ensure test DB is empty or use non-existent ID
  - [ ] Use a numeric ID that doesn't exist (e.g., 99999)
- [ ] Step 3: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Use `supertest` to create request client
- [ ] Step 4: Test endpoint with non-existent task
  - [ ] Make GET request to `/api/tasks/99999` (non-existent ID)
  - [ ] Verify response status is 404
  - [ ] Verify response body contains error information
- [ ] Step 5: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates task not found

### Specific Requirements

- [ ] Requirement 1: Error returns 404 status
  - Acceptance: HTTP status code is 404 when task not found
  - Acceptance: Error is caught and handled
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates task not found
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: ID exists but task was deleted
  - Verify 404 is returned

### Testing

- [ ] Write test in `tests/tasks-api.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining 404 handling

### Verification

- [ ] Verify test passes
- [ ] Verify error handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 7: HTTP Server - Tasks API
- **Execution Timing**: Must be completed after TASK-7.3.1
- **Dependencies**: TASK-7.3.1, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - 404 is appropriate for not found resources
  - Use real database (not mocked) for integration test
- **Task Independence**: Can be done after TASK-7.3.1

## Related Tasks

- Previous: TASK-7.3.1
- Next: TASK-7.3.3
- Dependencies: TASK-7.3.1, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-7.3: GET /api/tasks/:id (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/tasks-api.test.ts`
2. Test passes
3. Error handling is verified (404 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want 404 errors so I know when tasks don't exist
- As a **client**, I want 404 responses so I can handle missing tasks

**Automated Tests**:
- Test verifies 404 status when task not found
- Test passes
- All tests pass












