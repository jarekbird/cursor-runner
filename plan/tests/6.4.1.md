# TASK-6.4.1: Test GET /api/agent/:id returns 404 when conversation not found

**Section**: 6. HTTP Server - Agent Conversation API
**Subsection**: 6.4.1
**Task ID**: TASK-6.4.1
**Parent Task**: TASK-6.4

## Description

Write a test to verify that `GET /api/agent/:id` returns 404 status code when the conversation is not found. This ensures proper error handling for non-existent conversations.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (GET /api/agent/:id endpoint), `src/agent-conversation-service.ts` (getConversation method)

## Current State

- GET /api/agent/:id endpoint exists
- AgentConversationService.getConversation() exists
- `tests/agent-conversation-api.integration.test.ts` exists
- Test for 404 handling may not exist yet

**Important**: This test verifies 404 error handling.

## Checklist

### Preparation and Setup

- [ ] Review GET /api/agent/:id endpoint implementation
- [ ] Review AgentConversationService.getConversation() method
- [ ] Review existing tests in `tests/agent-conversation-api.integration.test.ts`
- [ ] Use `createMockServer()` and `assertErrorResponse()` helpers

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'GET /api/agent/:id returns 404 when conversation not found'`
  - [ ] Use `describe` block for /api/agent/:id endpoint tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Mock `agentConversationService.getConversation()` to return null or throw not found error
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with non-existent conversation
  - [ ] Make GET request to `/api/agent/non-existent-id`
  - [ ] Verify response status is 404
  - [ ] Verify response body contains error information
- [ ] Step 4: Verify error response
  - [ ] Use `assertErrorResponse()` helper
  - [ ] Verify error message indicates conversation not found

### Specific Requirements

- [ ] Requirement 1: Error returns 404 status
  - Acceptance: HTTP status code is 404 when conversation not found
  - Acceptance: Error is caught and handled
- [ ] Requirement 2: Error message is clear
  - Acceptance: Error message indicates conversation not found
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Testing

- [ ] Write test in `tests/agent-conversation-api.integration.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining 404 handling

### Verification

- [ ] Verify test passes
- [ ] Verify error handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 6: HTTP Server - Agent Conversation API
- **Execution Timing**: Must be completed after TASK-6.3.4
- **Dependencies**: TASK-6.3.4, TASK-1.1.4, TASK-1.1.5
- **Important Considerations**: 
  - 404 is appropriate for not found resources
- **Task Independence**: Can be done after TASK-6.3.4

## Related Tasks

- Previous: TASK-6.3.4
- Next: TASK-6.4.2
- Dependencies: TASK-6.3.4, TASK-1.1.4, TASK-1.1.5
- Related:
  - TASK-6.4: GET /api/agent/:id (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/agent-conversation-api.integration.test.ts`
2. Test passes
3. Error handling is verified (404 status)
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want 404 errors so I know when conversations don't exist
- As a **client**, I want 404 responses so I can handle missing conversations

**Automated Tests**:
- Test verifies 404 status when conversation not found
- Test passes
- All tests pass








