### Step 11.2: ConversationService - Conversation Management
**Objective**: Test conversation creation and retrieval.

This task has been broken down into granular subtasks:

1. [TASK-11.2.1](./11.2.1.md): Test createConversation() sets TTL and last-conversation key per queue
2. [TASK-11.2.2](./11.2.2.md): Test forceNewConversation() clears last-conversation key and creates new
3. [TASK-11.2.3](./11.2.3.md): Test getConversationContext() returns messages
4. [TASK-11.2.4](./11.2.4.md): Test getConversationContext() prefers summarized messages when available

**Note**: Complete all subtasks in order (11.2.1 → 11.2.2 → 11.2.3 → 11.2.4). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Extend `tests/conversation-service.test.ts`:
   - Add test: `createConversation()` sets TTL and last-conversation key per queue
   - Add test: `forceNewConversation()` clears last-conversation key and creates new
   - Add test: `getConversationContext()` returns messages
   - Add test: `getConversationContext()` prefers summarized messages when available
   - Use mock Redis client

**Tests**:
- Verify conversation creation
- Verify TTL behavior
- Verify context retrieval

**Verification**:
- Tests pass

