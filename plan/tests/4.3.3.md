# TASK-4.3.3: Test POST /cursor/iterate uses provided maxIterations value

**Section**: 4. HTTP Server - Cursor Execution Routes
**Subsection**: 4.3.3
**Task ID**: TASK-4.3.3
**Parent Task**: TASK-4.3

## Description

Write a test to verify that `POST /cursor/iterate` uses the provided `maxIterations` value from the request body when it is specified. This ensures custom maxIterations values work correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /cursor/iterate endpoint, maxIterations handling)

## Current State

- POST /cursor/iterate endpoint exists
- maxIterations handling exists
- `tests/server.test.ts` exists
- Test for provided maxIterations may not exist yet

**Important**: This test verifies custom maxIterations parameter handling.

## Checklist

### Preparation and Setup

- [ ] Review POST /cursor/iterate endpoint implementation
- [ ] Review existing tests in `tests/server.test.ts`
- [ ] Use `createMockServer()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /cursor/iterate uses provided maxIterations value'`
  - [ ] Use same `describe` block as other cursor/iterate tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Mock `cursorExecution.iterate()` to track maxIterations parameter
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test iterate endpoint with maxIterations
  - [ ] Make POST request to `/cursor/iterate` with body: `{ prompt: 'test', maxIterations: 10 }`
  - [ ] Verify iterate() was called with maxIterations: 10
  - [ ] Verify response is successful
- [ ] Step 4: Verify provided value is used
  - [ ] Assert iterate() receives maxIterations: 10 (not default 5)
  - [ ] Assert provided value overrides default

### Specific Requirements

- [ ] Requirement 1: Provided maxIterations is used
  - Acceptance: iterate() is called with provided maxIterations value
  - Acceptance: Provided value overrides default
- [ ] Requirement 2: Value is passed correctly
  - Acceptance: maxIterations parameter is extracted from body
  - Acceptance: maxIterations is passed to iterate() method
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests
  - Acceptance: Mocks are properly cleaned up

### Error Handling and Edge Cases

- [ ] Handle edge case 1: maxIterations is very large
  - Verify large values are accepted (if within limits)
- [ ] Handle edge case 2: maxIterations is 1
  - Verify minimum value works

### Testing

- [ ] Write test in `tests/server.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Verify provided maxIterations is used correctly
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining maxIterations handling

### Verification

- [ ] Verify test passes
- [ ] Verify provided value handling is correct
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 4: HTTP Server - Cursor Execution Routes
- **Execution Timing**: Must be completed after TASK-4.3.2
- **Dependencies**: TASK-4.3.2, TASK-1.1.4
- **Important Considerations**: 
  - Provided value should override default
  - Value should be validated (if applicable)
- **Task Independence**: Can be done after TASK-4.3.2

## Related Tasks

- Previous: TASK-4.3.2
- Next: TASK-4.3.4
- Dependencies: TASK-4.3.2, TASK-1.1.4
- Related:
  - TASK-4.3: POST /cursor/iterate (Synchronous) (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/server.test.ts`
2. Test passes
3. Provided maxIterations is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want to specify maxIterations so I can control iteration limits
- As a **client**, I want custom maxIterations so I can adjust iteration behavior

**Automated Tests**:
- Test verifies provided maxIterations is used
- Test verifies value overrides default
- Test passes
- All tests pass








