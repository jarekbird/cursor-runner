# TASK-6.2.2: Test POST /api/agent/new saves metadata when provided

**Section**: 6. HTTP Server - Agent Conversation API
**Subsection**: 6.2.2
**Task ID**: TASK-6.2.2
**Parent Task**: TASK-6.2

## Description

Write a test to verify that `POST /api/agent/new` saves `metadata` when provided in the request body. This ensures conversation metadata is persisted correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/server.ts` (POST /api/agent/new endpoint), `src/agent-conversation-service.ts` (createConversation and updateConversation methods)

## Current State

- POST /api/agent/new endpoint exists
- Metadata handling exists (or needs to be implemented)
- `tests/agent-conversation-api.integration.test.ts` exists
- Test for metadata persistence may not exist yet

**Important**: This test verifies metadata persistence.

## Checklist

### Preparation and Setup

- [ ] Review POST /api/agent/new endpoint implementation
- [ ] Review AgentConversationService.createConversation() and updateConversation() methods
- [ ] Review existing tests in `tests/agent-conversation-api.integration.test.ts`
- [ ] Use `createMockServer()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'POST /api/agent/new saves metadata when provided'`
  - [ ] Use same `describe` block as other /api/agent/new tests
- [ ] Step 2: Set up mocks
  - [ ] Use `createMockServer()` helper
  - [ ] Mock `agentConversationService.createConversation()` to return test conversation ID
  - [ ] Mock `agentConversationService.updateConversation()` to track metadata updates
  - [ ] Use `supertest` to create request client
- [ ] Step 3: Test endpoint with metadata
  - [ ] Make POST request to `/api/agent/new` with body: `{ metadata: { key: 'value', custom: 'data' } }`
  - [ ] Verify createConversation() was called
  - [ ] Verify updateConversation() was called with metadata
  - [ ] Verify metadata is saved correctly
- [ ] Step 4: Verify metadata persistence
  - [ ] Assert metadata is passed to updateConversation()
  - [ ] Assert metadata structure is preserved

### Specific Requirements

- [ ] Requirement 1: Metadata is saved
  - Acceptance: updateConversation() is called with metadata
  - Acceptance: Metadata structure is preserved
- [ ] Requirement 2: Metadata is optional
  - Acceptance: Conversation can be created without metadata
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Metadata is empty object (verify if saved or ignored)
- [ ] Handle edge case 2: Metadata is null (verify handling)

### Testing

- [ ] Write test in `tests/agent-conversation-api.integration.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining metadata handling
- [ ] Document metadata structure

### Verification

- [ ] Verify test passes
- [ ] Verify metadata persistence works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 6: HTTP Server - Agent Conversation API
- **Execution Timing**: Must be completed after TASK-6.2.1
- **Dependencies**: TASK-6.2.1, TASK-1.1.4
- **Important Considerations**: 
  - Metadata should be persisted after conversation creation
  - Metadata structure should be flexible
- **Task Independence**: Can be done after TASK-6.2.1

## Related Tasks

- Previous: TASK-6.2.1
- Next: TASK-6.2.3
- Dependencies: TASK-6.2.1, TASK-1.1.4
- Related:
  - TASK-6.2: POST /api/agent/new (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/agent-conversation-api.integration.test.ts`
2. Test passes
3. Metadata persistence is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want metadata saved so I can store custom conversation data
- As a **client**, I want to save metadata so I can track conversation context

**Automated Tests**:
- Test verifies metadata is saved when provided
- Test verifies metadata structure is preserved
- Test passes
- All tests pass

