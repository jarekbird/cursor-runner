# TASK-13.2.3: Test validateGmailConfig() returns valid when all vars present

**Section**: 13. System Settings, Gmail MCP, Feature Flags
**Subsection**: 13.2.3
**Task ID**: TASK-13.2.3
**Parent Task**: TASK-13.2

## Description

Write a test to verify that `validateGmailConfig()` returns valid when all required environment variables are present. This ensures validation works correctly for complete configurations.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/system-settings-gmail-validation.ts` (validateGmailConfig method)

## Current State

- validateGmailConfig() exists
- Validation logic exists
- `tests/system-settings-gmail-validation.test.ts` exists
- Test for valid configuration may not exist yet

**Important**: This test verifies validation for complete configurations.

## Checklist

### Preparation and Setup

- [ ] Review validateGmailConfig() implementation
- [ ] Review required environment variables list
- [ ] Review existing tests in `tests/system-settings-gmail-validation.test.ts`
- [ ] Use test environment setup

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'validateGmailConfig() returns valid when all vars present'`
  - [ ] Use same `describe` block as other Gmail MCP tests
- [ ] Step 2: Set up test environment
  - [ ] Use `beforeEach` to save original env vars
  - [ ] Set all required Gmail MCP environment variables to test values
  - [ ] Use `afterEach` to restore original env vars
- [ ] Step 3: Test validateGmailConfig() with all vars
  - [ ] Call `validateGmailConfig()`
  - [ ] Verify validation result indicates valid
  - [ ] Verify no missing variables are reported
- [ ] Step 4: Verify validation result
  - [ ] Assert validation is valid
  - [ ] Assert no missing variables

### Specific Requirements

- [ ] Requirement 1: Validation is valid
  - Acceptance: Returns valid when all vars present
  - Acceptance: No missing variables reported
- [ ] Requirement 2: Validation is correct
  - Acceptance: All required vars are checked
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Error Handling and Edge Cases

- [ ] Handle edge case 1: Empty string values (verify if treated as missing)

### Testing

- [ ] Write test in `tests/system-settings-gmail-validation.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining validation

### Verification

- [ ] Verify test passes
- [ ] Verify validation works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 13: System Settings, Gmail MCP, Feature Flags
- **Execution Timing**: Must be completed after TASK-13.2.2
- **Dependencies**: TASK-13.2.2
- **Important Considerations**: 
  - All required vars must be present
  - Validation should be comprehensive
- **Task Independence**: Can be done after TASK-13.2.2

## Related Tasks

- Previous: TASK-13.2.2
- Next: TASK-13.2.4
- Dependencies: TASK-13.2.2
- Related:
  - TASK-13.2: Gmail MCP Configuration (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/system-settings-gmail-validation.test.ts`
2. Test passes
3. Validation is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want validation so I know when config is complete
- As a **system**, I want validation so I don't start with incomplete config

**Automated Tests**:
- Test verifies validation returns valid when all vars present
- Test passes
- All tests pass








