### Step 7.5: PUT /api/tasks/:id
**Objective**: Test task update endpoint.

This task has been broken down into granular subtasks:

1. [TASK-7.5.1](./7.5.1.md): Test PUT /api/tasks/:id returns 400 when id is not numeric
2. [TASK-7.5.2](./7.5.2.md): Test PUT /api/tasks/:id returns 400 when prompt is empty string
3. [TASK-7.5.3](./7.5.3.md): Test PUT /api/tasks/:id returns 400 when status is not a number
4. [TASK-7.5.4](./7.5.4.md): Test PUT /api/tasks/:id returns 400 when order is not a number
5. [TASK-7.5.5](./7.5.5.md): Test PUT /api/tasks/:id returns 404 when task not found
6. [TASK-7.5.6](./7.5.6.md): Test PUT /api/tasks/:id updates only provided fields
7. [TASK-7.5.7](./7.5.7.md): Test PUT /api/tasks/:id updates updatedat timestamp
8. [TASK-7.5.8](./7.5.8.md): Test PUT /api/tasks/:id returns updated task with status_label

**Note**: Complete all subtasks in order (7.5.1 → 7.5.2 → 7.5.3 → 7.5.4 → 7.5.5 → 7.5.6 → 7.5.7 → 7.5.8). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Extend `tests/tasks-api.test.ts`:
   - Add test: `PUT /api/tasks/:id` returns 400 when id is not numeric
   - Add test: `PUT /api/tasks/:id` returns 400 when `prompt` is empty string
   - Add test: `PUT /api/tasks/:id` returns 400 when `status` is not a number
   - Add test: `PUT /api/tasks/:id` returns 400 when `order` is not a number
   - Add test: `PUT /api/tasks/:id` returns 404 when task not found
   - Add test: `PUT /api/tasks/:id` updates only provided fields
   - Add test: `PUT /api/tasks/:id` updates `updatedat` timestamp
   - Add test: `PUT /api/tasks/:id` returns updated task with `status_label`

**Tests**:
- Verify all validation cases
- Verify partial updates
- Verify timestamp updates

**Verification**:
- Tests pass
- Updates work correctly

