# TASK-12.1.4: Test TaskService.getTaskById() returns task or null

**Section**: 12. Persistence & Migrations
**Subsection**: 12.1.4
**Task ID**: TASK-12.1.4
**Parent Task**: TASK-12.1

## Description

Write a test to verify that `TaskService.getTaskById()` returns the task when found or null when not found. This ensures task retrieval works correctly.

**Reference Implementation**: `virtual-assistant/cursor-runner/src/task-service.ts` (getTaskById method)

## Current State

- TaskService.getTaskById() exists
- Task retrieval exists
- `tests/task-service.test.ts` exists
- Test for getTaskById may not exist yet

**Important**: This test verifies task retrieval.

## Checklist

### Preparation and Setup

- [ ] Review TaskService.getTaskById() implementation
- [ ] Review existing tests in `tests/task-service.test.ts`
- [ ] Use `createTempSqliteDb()` helper

### Implementation Steps

- [ ] Step 1: Create test structure
  - [ ] Add test: `'getTaskById() returns task or null'`
  - [ ] Use same `describe` block as other basic operations tests
- [ ] Step 2: Set up test database
  - [ ] Use `createTempSqliteDb()` helper in `beforeAll`
  - [ ] Run migrations to set up schema
- [ ] Step 3: Test task retrieval
  - [ ] Test case 1: Create test task, call `getTaskById()` with task ID
  - [ ] Verify task is returned
  - [ ] Test case 2: Call `getTaskById()` with non-existent ID
  - [ ] Verify null is returned
- [ ] Step 4: Verify retrieval
  - [ ] Assert task is returned when found
  - [ ] Assert null is returned when not found

### Specific Requirements

- [ ] Requirement 1: Task is returned when found
  - Acceptance: getTaskById() returns task when ID exists
- [ ] Requirement 2: Null is returned when not found
  - Acceptance: getTaskById() returns null when ID doesn't exist
- [ ] Requirement 3: Test is isolated
  - Acceptance: Test doesn't affect other tests

### Testing

- [ ] Write test in `tests/task-service.test.ts`
- [ ] Run test: `npm run test:integration`
- [ ] Verify test passes
- [ ] Run full test suite: `npm test`
- [ ] Run type checking: `npm run type-check`
- [ ] Run linting: `npm run lint`
- [ ] **DO NOT manually test by running the server** - use automated tests

### Documentation

- [ ] Add test comments explaining task retrieval

### Verification

- [ ] Verify test passes
- [ ] Verify task retrieval works correctly
- [ ] Verify no regressions in existing tests

## Notes

- This task is part of Phase 12: Persistence & Migrations
- **Execution Timing**: Must be completed after TASK-12.1.3
- **Dependencies**: TASK-12.1.3, TASK-1.1.2, TASK-1.1.4
- **Important Considerations**: 
  - Should return task when found
  - Should return null when not found
- **Task Independence**: Can be done after TASK-12.1.3

## Related Tasks

- Previous: TASK-12.1.3
- Next: TASK-12.2.1
- Dependencies: TASK-12.1.3, TASK-1.1.2, TASK-1.1.4
- Related:
  - TASK-12.1: TaskService - Basic Operations (parent task)

## Definition of Done

**Definition of Done**: 
1. Test is written in `tests/task-service.test.ts`
2. Test passes
3. Task retrieval is verified
4. All checks pass (test, type-check, lint)
5. Code is committed to git with descriptive commit message
6. Code is pushed to origin

**User Stories**:
- As a **developer**, I want to retrieve tasks so I can access task data
- As a **client**, I want task retrieval so I can display task details

**Automated Tests**:
- Test verifies getTaskById() returns task or null
- Test passes
- All tests pass

