### Step 13.2: Gmail MCP Configuration
**Objective**: Test Gmail MCP validation.

This task has been broken down into granular subtasks:

1. [TASK-13.2.1](./13.2.1.md): Test getGmailMcpEnabled() returns false when env not set
2. [TASK-13.2.2](./13.2.2.md): Test getGmailMcpEnabled() returns true for various true values
3. [TASK-13.2.3](./13.2.3.md): Test validateGmailConfig() returns valid when all vars present
4. [TASK-13.2.4](./13.2.4.md): Test validateGmailConfig() returns missing list when vars absent
5. [TASK-13.2.5](./13.2.5.md): Test CursorRunner.validateGmailConfig() logs appropriately for all combinations

**Note**: Complete all subtasks in order (13.2.1 → 13.2.2 → 13.2.3 → 13.2.4 → 13.2.5). Each subtask is independently completable with its own Definition of Done.

**Original Tasks**:
1. Extend `tests/system-settings-gmail-validation.test.ts`:
   - Add test: `getGmailMcpEnabled()` returns false when env not set
   - Add test: `getGmailMcpEnabled()` returns true for various true values
   - Add test: `validateGmailConfig()` returns valid when all vars present
   - Add test: `validateGmailConfig()` returns missing list when vars absent
   - Add test: `CursorRunner.validateGmailConfig()` logs appropriately for all combinations
   - Test all combinations of `ENABLE_GMAIL_MCP` and required env vars

**Tests**:
- Verify enablement logic
- Verify validation
- Verify logging

**Verification**:
- Tests pass
- Gmail config validation is comprehensive

